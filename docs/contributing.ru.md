# Руководство разработчика / Contributing Guide

Это руководство описывает, как вносить изменения в `magic-spec`, работать со скриптами, собирать и публиковать пакеты на npm и PyPI.

---

## Структура репозитория

```plaintext
magic-spec/                         # Корень репозитория
│
├── .magic/                         # 🔧 Движок SDD (источник истины)
├── .agent/                         # 🎯 Точки входа для AI-агентов
├── adapters/                       # 🔌 Адаптеры для конкретных AI-окружений
│   ├── cursor/
│   ├── github/
│   ├── kilocode/
│   └── windsurf/
│
├── installers/
│   ├── node/                       # 📦 npm-пакет (npx magic-spec)
│   │   ├── src/index.js            #    CLI: точка входа
│   │   ├── publish.js              #    Скрипт публикации на npm
│   │   ├── package.json            #    Конфиг npm-пакета + все скрипты
│   │   └── dist/                   #    Папка сборки (gitignored)
│   │
│   └── python/                     # 📦 PyPI-пакет (uvx magic-spec)
│       ├── magic_spec/
│       │   ├── __init__.py
│       │   └── __main__.py         #    CLI: точка входа
│       ├── pyproject.toml          #    Конфиг PyPI-пакета
│       └── dist/                   #    Папка сборки (gitignored)
│
└── docs/                           # 📄 Документация
    ├── README.md                   #    EN: описание системы
    └── README.ru.md                #    RU: описание системы
```

---

## Node.js инстоллер (`installers/node/`)

Все команды выполняются из папки `installers/node/`.

### Процесс сборки

Перед публикацией необходима **сборка** — ассемблирование файлов из корня репозитория в папку `dist/`:

```bash
cd installers/node
npm run build
```

Команда `build`:

1. Удаляет старую `dist/` (если есть).
2. Копирует `index.js` и `package.json` в `dist/`.
3. Копирует `.magic/`, `.agent/`, `adapters/` из корня репозитория.
4. Копирует `README.md` и `LICENSE` из корня репозитория.
5. Копирует `package.json` в `dist/`.

После сборки `dist/` содержит всё необходимое для публикации.

### Справочник скриптов

| Скрипт | Команда | Описание |
| :--- | :--- | :--- |
| `npm run build` | `node -e "..."` | Собрать всё в `dist/` |
| `npm run check` | `build` + `npm pack --dry-run` | Проверить содержимое пакета без загрузки |
| `npm run publish` | `build` + `node publish.js` | Собрать и опубликовать на npm |
| `npm run publish:dry` | `build` + `npm publish --dry-run` | Имитация публикации (без реальной загрузки) |
| `npm run test:link` | `build` + `npm link` | Установить локально как глобальный пакет |
| `npm run test:pack` | `build` + `npm pack` | Создать `.tgz` архив пакета в `dist/` |
| `npm run version:patch` | `npm version patch` | Поднять версию: `1.0.0 → 1.0.1` |
| `npm run version:minor` | `npm version minor` | Поднять версию: `1.0.0 → 1.2.1` |
| `npm run version:major` | `npm version major` | Поднять версию: `1.0.0 → 2.0.0` |

### Локальное тестирование (до публикации)

**Метод A — npm link** (быстрее всего, работает как глобальная установка):

```bash
cd installers/node
npm run test:link
# Теперь команда magic-spec доступна глобально

# Тестируем в любом проекте:
cd C:\Users\Oleg\Desktop\test-project
magic-spec
magic-spec --env cursor

# Удалить после тестирования:
npm unlink -g magic-spec
```

**Метод B — tarball** (ближе всего к реальному опыту через npx):

```bash
cd installers/node
npm run test:pack
# Создаст dist/magic-spec-1.0.0.tgz

# Установить в тестовый проект:
cd C:\Users\Oleg\Desktop\test-project
npm install D:\Projects\src\github.com\teratron\magic-spec\installers\node\dist\magic-spec-1.0.0.tgz
npx magic-spec
```

**Метод C — прямой запуск** (самый быстрый цикл итерации):

```bash
# Прямо из корня репозитория — никакой установки не нужно:
node installers/node/index.js
node installers/node/index.js --env cursor
node installers/node/index.js --update
```

### Публикация на npm

#### Первый раз (регистрация имени)

```bash
# 1. Авторизуйтесь в npm (один раз, сохраняет сессию в системе)
npm login

# 2. Соберите и опубликуйте
cd installers/node
npm run publish
```

#### Последующие публикации

После `npm login` сессия сохраняется. Просто:

```bash
cd installers/node
npm run publish
```

#### Сухой прогон (проверка без загрузки)

```bash
cd installers/node
npm run publish:dry
```

#### Повышение версии

```bash
# Поднять patch-версию (баг-фиксы)
npm run version:patch

# Поднять minor-версию (новые фичи)
npm run version:minor

# Поднять major-версию (ломающие изменения)
npm run version:major

# ⚠️ Синхронизируйте версию с pyproject.toml вручную!
```

#### Авторизация в npm

```bash
# Один раз — откроет браузер для подтверждения
npm login
```

Сессия сохраняется в `~/.npmrc` до явного `npm logout`.

---

## Python инстоллер (`installers/python/`)

Все команды выполняются из папки `installers/python/`.

### Процесс сборки

```bash
cd installers/python
uv build
```

Команда `uv build` (используя `hatchling` как build-backend):

1. Собирает исходный дистрибутив (`.tar.gz`).
2. Собирает wheel (`.whl`).
3. Кладёт оба файла в `dist/`.

### Справочник команд

| Команда | Описание |
| :--- | :--- |
| `uv build` | Собрать пакет в `dist/` (`.whl` + `.tar.gz`) |
| `uv publish` | Опубликовать на PyPI (интерактивный ввод токена) |
| `uv publish --token pypi-...` | Опубликовать с токеном |
| `pip install -e .` | Editable-установка для разработки |
| `uv run python -m magic_spec` | Запустить прямо без установки |

### Синхронизация движка из корня репозитория

Перед сборкой нужно убедиться, что в `installers/python/` есть актуальные `README.md` и `LICENSE`:

```bash
cd installers/python

# Синхронизировать README.md и LICENSE из корня:
python -c "import shutil, pathlib; root=pathlib.Path('../..'); [shutil.copy2(root/f, pathlib.Path('.')/f) for f in ['README.md','LICENSE'] if (root/f).exists()]"
```

### Локальное тестирование (до публикации)

**Метод A — editable install** (быстрее всего, изменения видны мгновенно):

```bash
cd installers/python
pip install -e .

# Тестируем в любом проекте:
magic-spec
magic-spec --env cursor
magic-spec --update
```

**Метод B — wheel install** (ближе к реальному опыту через uvx):

```bash
cd installers/python
uv build
pip install dist/magic_spec-*.whl
magic-spec
```

**Метод C — прямой запуск** (без установки):

```bash
cd installers/python
uv run python -m magic_spec
uv run python -m magic_spec --env cursor
```

### Публикация на PyPI

#### Первый раз (регистрация имени)

```bash
cd installers/python

# 1. Синхронизировать README.md и LICENSE
python -c "import shutil, pathlib; root=pathlib.Path('../..'); [shutil.copy2(root/f, pathlib.Path('.')/f) for f in ['README.md','LICENSE'] if (root/f).exists()]"

# 2. Собрать
uv build

# 3. Опубликовать (имя пакета занимается при первом успешном push)
uv publish --token pypi-ВАШ_ТОКЕН
```

#### Последующие публикации

```bash
cd installers/python
uv build
uv publish --token pypi-ВАШ_ТОКЕН
```

#### Как получить PyPI-токен

1. Войдите на [pypi.org](https://pypi.org/).
2. Перейдите: **Account Settings** → **API tokens** → **Add API token**.
3. Имя: `magic-spec-deploy`.
4. Scope: **Entire account** (при первом пуше) или конкретный проект (после).
5. Скопируйте токен (начинается с `pypi-`) и передайте в `uv publish --token <токен>`.

#### Повышение версии

Версия задаётся вручную в `pyproject.toml`:

```toml
[project]
version = "1.0.1"  # ← поменяйте здесь
```

> ⚠️ **Важно:** Версии в `package.json` (npm) и `pyproject.toml` (PyPI) должны всегда совпадать!

---

## Чек-лист перед каждым релизом

```plaintext
□ Изменения закоммичены и запушены в git
□ Версия поднята в installers/node/package.json
□ Версия поднята в installers/python/pyproject.toml
□ Версии совпадают в обоих файлах
□ npm run publish:dry — прошёл без ошибок
□ Движок (.magic/, .agent/, adapters/) актуален
□ README.md в корне репозитория обновлён
□ npm run publish — опубликовано на npm
□ uv build && uv publish — опубликовано на PyPI
□ git tag vX.Y.Z && git push --tags — тег создан
```

---

## Частые проблемы

### `npm run publish` — "No such file or directory"

Убедитесь, что вы запускаете команду из директории `installers/node/`, а не из корня репозитория.

### `uv build` — "Readme file does not exist"

Скопируйте `README.md` из корня в `installers/python/`:

```bash
cp ..\..\README.md .
```

### `uv publish` — "No files found to publish"

Сначала запустите `uv build` в директории `installers/python/`.

### `npm login` — завис

npm перенаправляет в браузер для подтверждения. Проверьте браузер — там должна быть страница авторизации с кодом.

### Версия уже существует на npm/PyPI

npm и PyPI **не позволяют** перезаписать опубликованную версию. Поднимите версию (`patch`, `minor` или `major`) и публикуйте снова.

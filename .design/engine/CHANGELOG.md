# Engine Workspace Changelog

## Phase 28 — 2026-08-28 (Silent-Failure Pair: Link Coverage & Regeneration Trigger)

- Closed the two engine defects Phase 27 hit in production (retrospective R25, R26) — the same defect twice: tooling that reported success while shipping stale output. Graduated directly from `RETROSPECTIVE.md`'s own recommendations with no `/magic.spec` pass; both remedies were concrete enough at recording time that planning wrote their governing spec sections directly — the first phase where that graduation pattern (previously Phases 20/21/25/26, all spec-sourced) applied to retrospective-sourced items.
- **R25 — linked-pair validation coverage.** `workflows/*.md` are per-file hardlinks to `.agents/workflows/*.md` — the tree `sync-skills` actually reads — but `validate-hardlinks.js` hardcoded only two groups (AGENTS family, `rules/`) and the `[C-001]` constraint text named one directory. `l2-agent-surface.md` (1.0.1 → 1.1.0) gains §4, the closed three-group inventory (AGENTS family, `rules/`, `workflows/`) plus the invariant that every declared pair must be validated, and the explicit exclusion of `skills/` as NOT a linked pair (independently generated by `sync-skills` into both trees — verified live, one hardlink, not two). `dev/scripts/validate-hardlinks.js` replaced its hardcoded rules-only function with a table-driven `validateDirectoryPairLinks()` covering both `rules/` and the new `workflows/` group. `STATE.md`'s `[C-001]` widened to name all three groups as one closed class.
- **R26 — skill regeneration trigger.** The obvious repair for R25 was itself blocked by a second defect: `update-engine-meta` reported *"No changes detected in engine core"* and skipped regeneration entirely, because its detection reads `.magic/` checksums only while the outstanding change lived in `workflows/`. Planning read `update-engine-meta.js` before accepting the retrospective's own two-option framing at face value: widening the checksum manifest is barred by an existing, documented contract (`workflows/`/`skills/`/`rules/` are excluded on purpose, so partial installations stay supported) — so the fix binds the regeneration *trigger*, not the manifest. `l2-skill-wrappers.md` (1.3.0 → 1.4.0) gains §3.2 stating the invariant; `.magic/scripts/update-engine-meta.js` extracts `syncSkillWrappers()` and calls it unconditionally on the write path even when `.magic/` itself is unchanged, while `--check` stays strictly read-only (it is hardcoded into the user's pre-commit hook — a write side effect there would turn every commit into an engine mutation). The misleading "No changes detected in engine core" message is reworded to name the scope actually checked.
- Coverage: `dev/tests/engine.js` 69 → 72 tests, all three negative-controlled — each confirmed to fail against reverted/pre-fix code before being confirmed green against the fix. The `validate-hardlinks.js` case ran the actual pre-fix source (`git show HEAD`), not a reconstruction, against the same fixture. `l2-test-suite.md` 1.16.0 → 1.17.0.
- Found and fixed along the way: a doc comment containing the literal path example `` skills/*/SKILL.md `` — the `*/` sequence terminates a JS block comment early — caught by `node -c` before any task reached Done, reworded to `skills/{name}/SKILL.md`. Also empirically confirmed that `fs.writeFileSync()` on Windows/NTFS rewrites through an existing hardlink's shared inode rather than breaking it; every negative-control fixture uses unlink-then-write instead, matching the actual failure mode a write-replace editor produces.
- Engine 2.1.79 → 2.1.80 (C14, 70 files, no `--workflow` tag — only `update-engine-meta.js` fell inside `.magic/`). Validation: `update-engine-meta --check` exit 0; `validate-hardlinks.js` exit 0 across all three groups (workflows/ 7/7, rules/ 1/1, AGENTS 3/4 — the pre-existing, unrelated `GEMINI.md` gap); harness 72/72; `check-prerequisites --verify-headers` clean. Plan complete — no active phase remains; Retro L2 run (Session 9), signal restored to 🟢.

## Phase 27 — 2026-08-28 (Idea Intake Gate Deployment, E6)

- Deployed the input-side clarification gate specified by `l1-idea-intake-gate.md` v1.0.0. The engine already routed every artifact it *authors* through the `prompt-engineer` quality gate and reviewed nothing it *receives*, though the user's idea is the first prompt in the chain; an essence-level defect there is not deferred by a `TBD` marker but committed by the surrounding prose and inherited by plan, tasks, and code.
- `.magic/spec.md`: new `### Step 0.5: Idea Intake Gate (E6)` between Step 0 and Explore Mode — silent by default (IK-1), fires only on F1 incoherence or F2 essence ambiguity decided by the one-sentence-Overview test (IK-4), asks only intent-layer questions (IK-3), phrases them for a non-specialist (IK-5), terminates by strict shrink of the open-question set (IK-6), and leaves no artifact (IK-7).
- Three existing clauses stated the opposite rule and were reconciled — one more than the spec's own deployment table listed. Core Invariant 12 (Anti-Stall) now suspends only during an active convergent dialogue; the Dispatch `Ambiguity (C25)` constraint carves out E6 while keeping TBD-by-default for everything detail-level; and Mode Transition's `Auto-Transfer` cap was rebound from a one-round count ("never stall on a 2nd 'are you sure?' cycle") to non-convergence, which is what IK-6 actually requires. The Completion Checklist gained an `Idea Intake (E6)` entry.
- C27 whitelist entry **E6** registered atomically across all four carriers: the DA-2 table in `l1-decision-autonomy.md` (1.2.0 → 1.3.0, closure clause rescoped E1–E5 → E1–E6, plus a containment paragraph establishing that E6 does not loosen DA-1), `.design/RULES.md` (1.9.0 → 1.10.0), `.magic/templates/rules.md`, and `rules/magic.md` §7. A constitution that disagrees with the engine enforcing it is a release blocker, so the two rules files carry an identical narrowing note.
- `prompt-engineer` role card and `l2-role-cards-governance.md` (1.1.1 → 1.2.0): conditional six-row Idea Intake Gate audit (IK-2 investigation discharged, IK-3 intent-only, IK-4 firing justified, IK-5 wording, IK-6 convergence, IK-7 residency), gated on the Step 0.5 gate having actually fired — auditing a silent gate is itself an anti-pattern.
- `docs/spec.md` §5.0: plain-language explanation for users — the two situations that trigger a question, the guarantee that nothing technical is ever asked, and the "you decide" exit that closes the gate immediately. Two stale one-round-cap statements corrected in the same pass.
- Coverage: `dev/tests/suite.md` T209–T212 (v1.9.75 → v1.9.76) — silent pass, F2 fire with intent-only wording, non-convergent termination including the close-one-open-two case, and a technical fork proving no realization question reaches the user. `l2-test-suite.md` 1.15.0 → 1.16.0 corrects the recorded count 206 → 211; it was already one behind, since T208 shipped without a spec sync.
- Engine 2.1.78 → 2.1.79 (C14, 70 files, `--workflow magic.spec`). **Two engine defects surfaced during the bump and were worked around, not fixed.** `workflows/*.md` are per-file hardlinks to `.agents/workflows/*.md`, a pair guarded by neither the `[C-001]` constraint text nor `validate-hardlinks.js`; the wrapper edit delinked it, and `sync-skills` — which reads the `.agents/` side — generated the shipped skill wrapper from pre-edit content. Re-running `update-engine-meta` then reported "No changes detected in engine core" and skipped regeneration entirely, because its change detection reads `.magic/` checksums only and the outstanding change lived in `workflows/`. Link restored (7/7 workflow pairs at two links), `sync-skills` invoked directly, and the generated `SOURCE:` header flipped back from `.agents/workflows/magic.spec.md` to the canonical `workflows/magic.spec.md`, confirming the repair. Recorded as `WORKFLOWS_HARDLINK_UNGUARDED` (error) and `C14_SKIPS_WORKFLOWS_ONLY_CHANGE` (warning).
- `[C-001]` fired exactly as planned for `rules/magic.md`: `fsutil` reported one link and hashes diverged immediately after the edit; recreated and verified. An unrelated pre-existing delink (`GEMINI.md`, 3/4 in the AGENTS family) was found by the same validator run and recorded, not repaired.
- Validation: `update-engine-meta --check` exit 0; `validate-hardlinks.js` exit 0; harness 69/69; `check-prerequisites` clean. The gate ships no executor code path, so its coverage is cognitive-only by design and the flat harness count is not an omission.

## Phase 26 — 2026-08-27 (Commit-Suggestion Feature Removal)

- By explicit user directive, the engine no longer composes or prints a suggested commit message on any `finalize` path — committing (timing, grouping, message) is left entirely to the user, with no engine involvement even at the suggestion level. The pre-existing hard rule (no write-side git operation is ever invoked) is unaffected.
- `.magic/scripts/lib/commit-suggester.js`: removed `deriveType()`, `deriveScope()`, `artifactId()`, `buildSummary()`, `buildCommitMessage()` — the module now composes only the CHANGELOG bullet (`deriveChangelogCategory()`, `buildChangelogBullet()`), unaffected by this retirement. File not renamed.
- `.magic/scripts/finalize.js`: dropped the `--no-commit-msg` CLI flag, the `suggestCommit` config read, `emitFallbackCommitSuggestion()` and its skip-path call, the `commitMsg` construction block, `emitSuccess()`'s `### Suggested commit message` rendering, and `emitTail()`'s `Auto-commit is disabled by design` notice. `emitTail()` rewritten to join its remaining optional sections (diagnostics digest, next step) with single blank-line separators — the removed notice had been the de-facto (unconditional) spacing anchor, so a literal deletion would have produced a double-blank-line regression once both remaining sections were present.
- `.design/workspace.json` and `.magic/templates/workspace.json`: dropped the now-dead `finalization.suggestCommit` key.
- Spec layer already retired the corresponding invariants in the `/magic.spec` pass immediately preceding this phase: `l1-session-continuity.md` 1.11.0 → 2.0.0 (SC-3/SC-3.1 retired), `l2-engine-finalization.md` 2.0.0 → 3.0.0 (§5.2 rewritten, §8 terminal block simplified), `l2-finalize-output-contract.md` 1.2.0 → 2.0.0 (§3 rescoped to stdout-listing completeness alone), plus cross-reference-only patches to `l2-finalize-state-accuracy.md`, `l2-status-command.md`, `l2-test-suite.md`, `l1-engine-diagnostics.md`, `l2-engine-diagnostics.md`.
- Harness: deleted the now-obsolete `buildCommitMessage` unit test; rewrote the skip-path SC-2 test and the stdout-listing-completeness test to assert the commit-message output's absence instead of its presence; dropped `suggestCommit: true` from `createFinalizeFixture()`'s config fixture. 69 → 68 tests — the first net-negative harness count in this plan's history, traced entirely to a deleted capability's dedicated test, not a coverage loss.
- Engine 2.1.75 → 2.1.76 (C14, 70 files — `finalize.js`, `lib/commit-suggester.js`, `templates/workspace.json`, no `--workflow` tag). Validation: harness 68/68; `update-engine-meta` completed, snapshot synced `2.1.76`; `check-prerequisites` ok with an empty warnings array. Dry-run smoke test confirmed zero case-insensitive matches for "commit" in finalize's stdout outside a task's own title.

## Phase 25 — 2026-08-22 (CHANGELOG Dedup Discoverability Hint)

- `finalize.js`'s `emitSuccess()`: the `CHANGELOG` stdout row's deduped branch now names the remedy — `skipped (duplicate — run 'release-changelog' to rotate [Unreleased])` instead of the bare `skipped (duplicate)`. No change to bullet content, `bulletExists()`'s dedup logic, or `buildChangelogBullet()` — RC-11 is untouched.
- Field report (engine 2.1.73, consumer workspace `engine`) reproduced the already-documented §4.1 CHANGELOG vocabulary-exhaustion defect via the `run` case's single-item branch and proposed interpolating a phase/task identifier into the bullet — rejected as a direct RC-11 violation already barred by §4.4 constraint 1. The real gap: §4.4's `release-changelog` remedy (Phase 20) already exists and is fully reachable through `executor.js`'s generic dispatch, but `finalize.js`'s own stdout never told the operator it exists.
- New regression test (`dev/tests/engine.js`) is the first in this harness to exercise the CHANGELOG-write branch through a live CLI invocation — every prior test either asserted `buildChangelogBullet()`'s return value directly or ran with `createFinalizeFixture()`'s default `autoChangelog: false`. Two sequential real `finalize.js --workflow=run` runs: first appends normally (control, no hint), second (same single-`/tasks/`-file shape, same bullet) dedupes and names `release-changelog`. Negative-controlled via `git stash` on `finalize.js` alone — confirmed the test fails against pre-fix code before restoring.
- **Self-referential proof**: the `/magic.spec` dispatch that authored this fix's own spec hit the exact deduped-without-hint condition live during its own finalize run, before the fix existed — recorded via `record-diagnostic` (`CHANGELOG_DEDUP_HINT_MISSING`).
- `l2-finalize-output-contract.md` 1.1.0 → 1.2.0: new §4.5 documents the field report, the rejected hypothesis, and the Required Fix.
- Harness 68 → 69 tests.
- Engine 2.1.74 → 2.1.75 (C14, 70 files — `finalize.js` only, no `--workflow` tag). Validation: harness 69/69; `update-engine-meta` completed, snapshot synced `2.1.75`; `check-prerequisites --verify-headers` ok with an empty warnings array.

## Phase 24 — 2026-08-22 (Dev-Repo Engine-Version Snapshot Sync)

- New `dev/scripts/sync-engine-snapshot.js`: L2 writer patching `.design/INDEX.md`'s `**Engine Version:**` field to the current `.magic/.version`. `update-engine-meta.js`'s C14 write branch now delegates to it via the exact `dev/scripts/` presence guard the file already uses twice (`sync-skills.js`, `generate-checksums.js`) — no second detection mechanism. Consumer installations (guard absent) are unaffected: version bumps, snapshot stays untouched, a `warning`-severity diagnostic records the skip. `--check` mode (the user's pre-commit hook) is unaffected by construction — the call sits after `bumpVersion()`, inside the write path only. Closes a Parked backlog item open since 2026-06-12, revisited at explicit user request: Engine Upgrade Detection's snapshot exists to catch an *external* engine replacement in a consumer project, but in this engine's own repo every bump is first-party, so the drift line was self-inflicted noise re-narrated every `/magic.*` invocation until an `/magic.analyze` happened to run.
- `rules/magic.md` §1's "Snapshot contract" sentence amended to carve out the dev-repo exception; the external-drift contract for every consumer project is otherwise unchanged, word for word. Hardlink to `.agents/rules/magic.md` broke on edit (as documented, [C-001]) and was recreated and re-verified (`fsutil hardlink list`, `Get-FileHash` match, `validate-hardlinks.js` clean).
- **Regression found and fixed mid-phase, by the phase's own planning run**: `finalize.js`'s `synthesizeNextAction()` tier-2 loop (added in Phase 23's SC-2.1(c) per-item scan) took a task's display title from `stripQuoted()`-stripped content, so any inline code span in a title was blanked out of the persisted `Next Action` — first observed live when this phase's own `/magic.task` finalize printed `Execute T-24A01 New  (L2 snapshot writer)` with the backticked path missing. Fixed: the scan is now line-indexed — the SH-1 detector still reads stripped text (deciding which lines are real checklist items), but the title is recovered from the raw source at the same line index, since `stripQuoted()` preserves line count, not intra-line character offsets. No spec amendment required — `l2-finalize-state-accuracy.md` §9's contract never asked for the title to be stripped; the implementation over-applied a plan-time hardening.
- Two implementation bugs caught in the harness itself while adding coverage, both before being marked Done: a new dev-repo/consumer regression case initially asserted against `execSync`'s stdout-only return value, silently missing the `console.warn` (stderr) message it existed to pin — fixed with an explicit `2>&1` redirect. Both new cases were negative-controlled via `git stash` on the relevant source file alone, confirming each fails against pre-fix code before being restored.
- **Live end-to-end proof, not merely unit-tested**: T-24T02's own C14 bump was the first real invocation of the new path. `.design/INDEX.md`'s snapshot correctly jumped `2.1.72` → `2.1.74` directly, skipping the intermediate `2.1.73` recorded during Phase 23 (before this fix existed) — the field records current state, not a log, exactly as predicted at plan time.
- Harness 66 → 68 tests (dev-repo/consumer branch coverage for the snapshot sync; code-span title-preservation regression with an SH-1 detector control).
- Engine 2.1.73 → 2.1.74 (C14, 70 files — `update-engine-meta.js`, `finalize.js`). Validation: harness 68/68; `update-engine-meta --check` clean; `check-prerequisites --verify-headers` ok with an empty warnings array; `.design/INDEX.md`'s `**Engine Version:**` matches `.magic/.version` with zero manual edits.

## Phase 23 — 2026-08-22 (Next-Action Task-Level Precedence & Decision-Prune Honesty)

- `finalize.js`'s `synthesizeNextAction()` tier-2 loop no longer returns a phase's first open checklist line unconditionally: a new `isTaskExcluded()` reads that specific task's own `## Detailed Tracking` entry and skips it when `Status: Blocked` or `Assignment: User`, continuing the scan (within the phase, then across phases) for the next agent-actionable item. Closes the same class of self-contradiction SC-2.1(a) already closed at the phase level — a phase in good standing could still surface, as its first open line, a task its own tracking said the agent should not run. New terminal branch when every open task is excluded: names the first excluded task without recommending it as `/magic.run`-executable, and does not fall through to the plan-complete funnel (tasks remain — they are just not the agent's to run). Field report, engine 2.1.72, reproduced directly against that version.
- `update-state.js`'s decision-prune preamble no longer claims an archival to `PLAN.md` the code has never performed — the routine prune path (line-cap guard, oldest `## Recent Decisions` entry) simply deletes the line, and no code path in the file writes `PLAN.md`. Comment corrected to state actual behavior (dropped, not archived; pointed at `PLAN.md`/`CHANGELOG.md` for phase history instead). Same fix applied to `.magic/templates/state.md`, the bootstrap seed a brand-new workspace copies verbatim before any decision has ever been added — a second source for the same false claim, not separately reported but sharing the identical root string. Field report, engine 2.1.72, reproduced directly.
- `l2-finalize-state-accuracy.md` 1.1.0 → 1.1.1: corrected a stale §12 claim ("§8's coverage obligation is open") that Phase 19 had already closed, and the Overview's "not yet implemented" claim for §9/§10 — both went stale as a direct, same-session consequence of this phase landing the fixes they describe.
- Both `Required Fix` blocks were authored by the `/magic.spec` pass immediately preceding this planning invocation and both were reproduced directly against engine 2.1.72 twice — once during spec authoring (against unfixed code) and once more against the patched code, since the first implementation attempt at each fix had its own bug (see below).
- Two bugs found and fixed during implementation, before either task was marked Done: (1) the Detailed Tracking block-extraction regex used a lookahead ending in bare `$`, which under the `m` flag matches before *any* newline — not only end-of-string — so it matched immediately after the heading's own blank line and collapsed the capture to empty, silently defeating the whole screen; fixed with a flag-independent end anchor `(?![\s\S])`. (2) The realigned harness assertion first read "is dropped" against code emitting "entries **are** dropped" — a plural-agreement typo that looked byte-identical on visual inspection but failed `RegExp.test()`; caught by running the harness rather than trusting the edit.
- End-to-end validation (T-23T04) replayed both original field-report symptoms against the real `finalize.js` CLI, not only unit fixtures: `--dry-run` named the actionable task while excluding the blocked/user-assigned one; a real run's decision-prune emitted the corrected preamble with no archival claim.
- Harness 65 → 66 tests (1 new: task-level Blocked/Assignment precedence across four scenarios — first-item-Blocked, first-item-Assignment:User, all-excluded terminal branch, and the missing-Assignment-defaults-actionable negative control; 1 existing assertion realigned).
- Engine 2.1.72 → 2.1.73 (C14, 70 files — `finalize.js`, `update-state.js`, `templates/state.md`, no `--workflow` tag). Validation: harness 66/66; `update-engine-meta --check` clean; `check-prerequisites --verify-headers` ok with an empty warnings array.

## Phase 22 — 2026-08-13 (Field-Report Triage — DESIGN_DEBT_PENDING Structural Predicate, Mode C Depth Control, Project-Auditor Citation)

- `check-prerequisites.js`'s `planComplete` pre-check now recognizes a completed `## Active Phases` table by row status (every row `Done`/`Done (Archived)`/`Cancelled`, none non-terminal), not only by the literal `*None ...*` empty marker. Under the canonical single-table `tasks.md` template — the shape the shipped template actually produces, with no separate "completed" section — `phase-archiver.js` rewrites a finished row's status in place, so the literal-marker form could never reappear once any phase had ever been archived: `DESIGN_DEBT_PENDING` was structurally unreachable for the rest of a workspace's life, independent of Backlog content. Distinct from and not covered by Phase 21's Backlog Disposition counter fix — that fix corrected the *counting* rule; this one corrects the *plan-complete* predicate feeding it. Field report, engine 2.1.71 (reproduced against a 24/24-phases-Done workspace with 2 open Backlog bullets, gate silent).
- New regression case pinning the fix against the canonical single-table layout — the existing `DESIGN_DEBT_PENDING` fixtures had been built against a hand-split `## Active Phases` + `## Completed Phases` layout that no shipped script (template, archiver, or otherwise) has ever produced; that undocumented shape is why the defect above survived unnoticed through 21 prior phases.
- `.magic/analyze.md`'s Mode C ("Project Ventilation") now explicitly bypasses Core Invariant 6 (Depth Control) in its Audit Policy, and the Mode C Completion Checklist gained a non-halting advisory line for parity with Mode A/B. The prior text left it genuinely ambiguous whether a >500-file Mode C run must pre-scan-HALT — confirmed during implementation that Depth Control had in fact never been wired into Mode C's own step list at all (only into the Shared Pre-flight for Modes A/B), so the fix closes a textual contradiction rather than a live behavioral bug. Field report, engine 2.1.71, reproduced on this repository's own 763-file tree.
- `.magic/roles/project-auditor.md` §Operating Protocol step 5 corrected: "Verify anti-fabrication (Invariant 6 from analyze.md)" cited a non-existent invariant — `analyze.md`'s actual Invariant 6 is Depth Control (file-count HALT thresholds), unrelated to anti-fabrication. Now cites `.design/RULES.md` C13 §5 (Anti-Hallucination Audit), the real governing concept.
- All three fixes were fully specified by the `/magic.spec` pass that immediately preceded planning — Pre-flight for both `/magic.task` and `/magic.run` returned `ok: true` with no `DESIGN_DEBT_PENDING`.
- Harness 64 → 65 tests (1 new: canonical single-table `DESIGN_DEBT_PENDING` recognition across all-archived, mixed-status, and all-`Cancelled` fixtures).
- Engine 2.1.71 → 2.1.72 (C14, 70 files — `analyze.md`, `roles/project-auditor.md`, `check-prerequisites.js`, tagged `magic.analyze`). Validation: harness 65/65; `update-engine-meta --check` clean.

## Phase 21 — 2026-08-07 (check-prerequisites.js Registry-Scan Hygiene & Backlog Counter)

- Bound the three `INDEX.md`-side `specifications/` scan sites Phase 17 didn't reach to the same `stripQuoted()` + filename-grammar-bounded pattern already validated on the `PLAN.md` side: the registry cross-reference (`GHOST_REGISTRY`/`NAMING_VIOLATION`/`ORPHANED_SPEC`), the Layer Integrity parent lookup (`RULE_57_VIOLATION`), and the header-parity lookup (`VERSION_DRIFT`/`STATUS_DRIFT`). The pre-fix `/specifications\/([^)]*\.md)/g` had no newline exclusion, so a bare `specifications/` mention in prose with no immediate closing `)` could run the capture past the paragraph it started in — the exact shape a field report against engine 2.1.70 described (did not reproduce against this registry's own current content, but the code defect was real, and the governing spec's own compliance table had overstated it as already fixed). Hoisted the shared pattern to a module-level `SPEC_FILENAME_SRC` source string per SH-5 (one implementation, not four copies of the literal).
- `DESIGN_DEBT_PENDING`'s `openItems` count now excludes Backlog bullets carrying a trailing `*(Parked — {reason})*` marker — the Backlog Disposition Convention this session's `/magic.spec` pass defined after the gate re-fired on a Backlog with nothing left to design. Closed items (resolved/rejected/superseded) fold into the Backlog's own intro note and lose their bullet entirely; Parked items (design already answered, kept visible on purpose) carry the marker instead.
- Both fixes were authorized directly as Engine Improvement rather than through a full spec-then-plan cycle: both were already fully specified with no remaining design work, and a second same-session `DESIGN_DEBT_PENDING` HALT with an empty design queue was itself the evidence.
- Harness 62 → 64 tests (2 new: INDEX.md-side registry-scan hygiene with a positive control against a genuinely broken registered spec; Parked-marker exclusion across mixed/all-Parked/bare-word cases).
- Engine 2.1.70 → 2.1.71 (C14, 70 files — `check-prerequisites.js` only). Validation: harness 64/64; `update-engine-meta --check` clean; `check-prerequisites --verify-headers` ok with an empty warnings array.

## Phase 2 — 2026-08-07 (Skill Projection & Agent Surface — belated closure)

- `/magic.run` Pre-flight discovered `tasks/phase-2.md` never carried YAML frontmatter (predates the convention entirely) and — independently — that its third task, `T-2T01` (validation tests for skill projection), was still `Todo` while `TASKS.md`'s registry had reported the whole phase `Done` since 2026-06-12. Neither the C8 auto-archiver (needs `status: Done` frontmatter to fire) nor any Select-step scan (the phase wasn't in `## Active Phases` at all) could have caught this — it required reading the phase file's own checklist against the registry's summary claim.
- `T-2T01` closed via retrospec, not new authorship: its original target (`.magic/tests/suite.md`) predates the L1/L2 split and no longer exists; the equivalent coverage lives at `dev/tests/suite.md` **T190 — Skill Projection Parity**, which independently satisfies all three `l2-skill-wrappers.md` §5 Invariants (Parity, Naming, Read-Only) plus the §3.1 Orphan Cleanup step. Cross-checked against a live generated skill (`skills/magic-analyze/SKILL.md`) to confirm T190's hyphenated-name expectation matches actual output.
- Frontmatter added to `tasks/phase-2.md` (`status: Done`, `provides`, `key_files`) so it now archives through the standard C8 mechanism instead of remaining a silent gap.
- Found and recorded (not fixed here — out of `/magic.run`'s write scope): `l2-skill-wrappers.md` §2.2's frontmatter example still shows `name: magic.{command}` (dot form) against the real hyphenated output — same drift class T190 already guards against, just never back-ported to the spec's own example.

## Phase 20 — 2026-08-07 (Backlog Implementation — Release Rotation & Coverage Denominator)

- New `.magic/scripts/release-changelog.js`: explicit, opt-in executor subcommand wrapping the already-existing `changelog-writer.js#releaseUnreleased()` — `--version`/`--date` default from `.design/.version` and today (UTC). Deliberately never called from `finalize.js` or any other workflow: magic-spec cannot observe a downstream consumer's release event (a pushed git tag, an `npm publish`, an App Store submission), so rotation is a deliberate maintainer action, never an automatic side effect. Closes the R11 remainder — `finalize.js` also drops its now-dead `releaseUnreleased` import, the concrete evidence §4.2 of the governing spec cited for the suppression bug.
- New `EXEMPT` classification in `analyze-coverage.js`: `.design/` bookkeeping/journal files (`PLAN.md`, `TASKS.md`, `STATE.md`, `CONTEXT.md`, `CHANGELOG.md`, `RETROSPECTIVE.md`, and any archived phase journal) are excluded from the coverage-percentage denominator entirely — not merely from the numerator, unlike `AMBIGUOUS` — while remaining visible in JSON output (`summary.exempt`) for auditability. On this repository's own `engine` workspace, reported coverage rose 85.4% → 96.5% as 24 SDD-process files stopped being counted as `UNCOVERED` source. `.magic/analyze.md`'s Confidence Taxonomy table and `coverage_percent` formula updated in parallel (five-level taxonomy now, not four).
- Both fixes trace to design decisions made the same session, immediately after Phase 19's own `DESIGN_DEBT_PENDING` signal (SC-2.4) fired for the first time in production — the signal correctly distinguished "plan complete with genuinely open backlog" from the surrounding mechanical-drift checks it was built alongside.
- Harness 57 → 62 tests (5 new: 3 for release-changelog rotation and distinguishability across a rotation window, 2 for EXEMPT classification and its effect — or lack of one — on the reported percentage).
- Engine 2.1.68 → 2.1.69 (C14, 70 files — `analyze.md`, `analyze-coverage.js`, `finalize.js`, `release-changelog.js`). Validation: harness 62/62; `update-engine-meta --check` clean; `check-prerequisites --verify-headers` ok with an empty warnings array.

## Phase 19 — 2026-08-07 (Finalization Contract Fixes)

- `phase-archiver.js`'s `updatePlanIndex()` now rewrites a self-labelling phase link (`[tasks/N.md](tasks/N.md)`) atomically — label and target together — before falling back to its pre-existing target-only pass for descriptively-labelled links. Closes R10, reproduced on five consecutive archivals (Phases 14-18), each corrected by hand until now. A bare prose mention of the same path is provably untouched: verified in the harness, not merely assumed.
- New `DESIGN_DEBT_PENDING` signal in `check-prerequisites.js`: fires only when `TASKS.md` positively marks plan-complete (the engine's own `*None — ...*` empty-state text, not merely an absence of phase rows) **and** `PLAN.md`'s `## Backlog` holds ≥1 open item. `.magic/task.md` Pre-flight raises the HALT this signal was missing, recommending `/magic.spec {ws}` through the door `rules/magic.md` §5 already sanctions — `computeNextAction()` and SC-2.2 are untouched. Closes R13 (`l1-session-continuity.md` SC-2.4).
- Root-caused the CHANGELOG duplicate-`### Changed`-heading anomaly via `git blame`/`git show` rather than further reasoning about the writer's current logic: two manual, pre-automation direct edits to `CHANGELOG.md` one week apart in 2026-05, neither going through `appendBullet()`. `insertIntoUnreleased()`'s own category lookup cannot produce this from a clean write path. No CHANGELOG fix was written — the investigation's own governing spec bars one until this diagnosis existed. Diagnosis routed to `/magic.spec` for the R11 follow-up.
- Closed the one regression-coverage gap the finalization decomposition explicitly left open: the `addDecision` harness case asserted only that a new entry appears somewhere under the `## Recent Decisions` heading (`[\s\S]*`, matching at any distance) — structurally incapable of catching the shipped fix's actual defect class (entry landing before the blank-line/comment preamble, not after). Replaced with four structural assertions; negative-controlled by reconstructing the pre-fix insertion logic and confirming the new assertions reject its output.
- Harness 55 → 57 tests (net: one new archival case, one new Pre-flight-signal case, one existing case's assertions replaced rather than added-beside).
- Engine 2.1.67 → 2.1.68 (C14, 69 files — exactly the three files this phase touched). Validation: harness 57/57; `update-engine-meta --check` clean; `check-prerequisites --verify-headers` ok with an empty warnings array.

## Phase 18 — 2026-08-07 (Engine Diagnostics Digest)

- New `.magic/scripts/lib/diagnostics.js`: `record()`/`read()`/`drain()` over a single global JSONL sink (`.design/.cache/diagnostics.jsonl`, matching `finalize-state.json`'s own precedent), plus `formatDigest()`/`summarize()` for rendering — dedup by `(severity, source, code)` with an occurrence count, capped at 15 with an omission line, `[]` for empty input so absence needs no gate at the call site. Root cause: `rules/magic.md` §3 binds the agent to relay finalize's **stdout** verbatim, while all 17 pre-existing non-fatal findings across 6 scripts were written only to **stderr** — invisible by contract, not by oversight.
- `finalize.js` gained a single shared `emitTail()`, called once on both exit paths, replacing two independent copies of the auto-commit notice. Renders: notice → diagnostics digest (omitted entirely when nothing was recorded) → next step. The next step is the exact `Next Action` string `updateSessionState()` already computes and persists to `STATE.md` — threaded through, never recomputed, closing a latent divergence between what was written to live memory and what the agent narrated separately.
- New `record-diagnostic` executor subcommand: the agent's own findings (spec ambiguities, engine bugs, containment leaks hit mid-run) now reach the same digest through the same shape, with `--source` defaulting to `agent`. Always exits 0, including on a malformed finding — reporting a complaint must never itself fail.
- All 17 non-fatal emitters across `finalize.js`, `update-state.js`, `update-engine-meta.js`, `lib/project-version.js`, `init.js`, and `check-prerequisites.js` now record alongside their existing print, additive by design — a run that crashes before the drain still leaves its stderr trail. `check-prerequisites.js`'s warning helper forwards its existing typed shape directly (`type` → `code`, `fix` → `remedy`) rather than minting new codes. Three of the seventeen are cases where the engine had silently altered state on its own initiative (a reserved `Next Action` substituted, a corrupted `.version` auto-healed, a missing template silently substituted) with no prior record inside the relay contract at all.
- `rules/magic.md` §9 now obligates the agent to record what it previously only narrated once in chat, so a finding from early in a long run still reaches the user at the end of it. §3's relay instruction needed no change — "display stdout verbatim" already covers the digest once it rides inside stdout.
- Harness 46 → 55 tests (9 new): collector round-trip and exactly-once delivery, never-throws against an unwritable sink, truncated-line tolerance, dedup/cap rendering, `--dry-run` non-consumption (DG-4.1) — and, the first harness cases in the suite whose subject is cross-path *ordering* itself: digest-before-next-step and next-step-last, asserted on both the skip and significant finalize exit paths, plus next-step string identity against the persisted `STATE.md` value and empty-sink silence on both paths.
- Engine 2.1.65 → 2.1.66 (C14, 69 files). Validation: harness 55/55; `update-engine-meta --check` clean; `check-prerequisites --verify-headers` ok with an empty warnings array — incidentally confirmed live mid-implementation against this repository's own real `ENGINE_INTEGRITY` findings before the bump cleared them.

## Phase 17 — 2026-08-07 (Scan Input Hygiene)

- Extracted one shared strip-before-match helper (`.magic/scripts/lib/scan-hygiene.js`, `stripQuoted()`) and re-pointed both existing independent copies at it — `phase-archiver.js`'s archival-eligibility predicate and `update-state.js`'s two-level phase-checklist reader, the latter written during Phase 15, two phases before the pattern was named as a defect. Neither copy had drifted from the other yet; nothing would have revealed it if they had, which is the failure mode the consolidation closes.
- Bound the registry cross-reference in `check-prerequisites.js`: it now strips quoted content via the shared helper and bounds its capture to the filename grammar, fixing two compounding defects in one line — a Backlog entry that quoted template placeholder paths in code spans previously produced a `REGISTRY_MISMATCH` for a specification that does not exist, and a punctuation-bounded capture could merge several comma-separated filenames into one finding naming a token nobody wrote.
- Broadened `.magic/analyze.md`'s Link Integrity check from `.design/`-only to the full shipped tree, and excluded `.magic/templates/` from resolution: a template's paths are placeholders that resolve only once instantiated into a user project, so five prior findings against it were correctly-observed but wrongly-scoped.
- Stated the mention/use boundary once, as a precondition on the SDD Reference Containment scan's match-class list in `.magic/analyze.md`, rather than as a local carve-out re-discovered per audit — the same idea `l1-sdd-reference-containment.md`'s RC-9 states separately for its own illustrative examples, left untouched as a distinct, correctly-scoped carve-out.
- New specification `l1-scan-input-hygiene.md` (SH-1..SH-5), authored in the prior cycle after the pattern surfaced a fourth time; this phase closes it out and updates its own enforcement-surface table from mixed compliance to fully compliant, plus a row for the phase-checklist reader found while implementing.
- Harness 43 → 46 tests. Two initial test assertions were wrong, not the implementation: one expected zero registry findings when two correctly-bounded ones should fire for genuinely unquoted, unregistered mentions; one asserted no quoted checkbox survives anywhere in stripped output, overlooking that blockquotes and HTML comments are deliberately out of scope. Both caught by the first harness run and corrected in the same task.
- Engine 2.1.64 → 2.1.65 (C14, 67 files — includes the new shared helper). Validation: harness 46/46; `update-engine-meta --check` clean; `check-prerequisites --verify-headers` ok with an empty warnings array.

## Phase 16 — 2026-08-06 (Documentation Parity & Scaffold Boundary)

- Corrected every surface of `.magic/init.md` that claimed `init.js` bootstraps `STATE.md`. It does not — verified against the script, whose four write calls cover `workspace.json`, the global `INDEX.md`, `RULES.md`, and the workspace `INDEX.md`. `STATE.md` is created lazily by `update-state.js` on the first mutating command, which already owns template instantiation. WI-10's scope was broadened for exactly this reason: its original wording bound the layout diagram alone, while the Steps narrative and the Completion Checklist made the same claim unchecked. The verification step no longer names an artifact *count* — "all 6" was the brittle part, and "all 5" would only have reset the trap.
- Corrected the same inherited claim in three cognitive-suite scenarios (T01, T02, T58). Those scenarios are evaluated against the documented contract rather than by executing `init.js`, so they could never have caught the divergence — their own expected outcomes encoded it. Fixing the document without them would have left the suite asserting a fact its source of truth no longer supports.
- Added the RC-12 scaffold-removal check to ventilation as Mode C step 6a: a five-point structural inspection (build/lifecycle manifests, CI/CD configs, packaging include lists, product source and tests, product toolchain manifests) reporting `SDD_SCAFFOLD_COUPLING`, advisory and read-only on the leak scan's terms, with the same remediation owner. It exists because the token scan structurally cannot see this class — a build script invoking the engine names no task, no phase, no spec file, and reads as clean to every pattern-based surface while making the scaffold load-bearing. Numbered 6a rather than renumbering the following steps, which are cross-referenced by number elsewhere.
- Stated the scaffold framing in the ambient agent rules: §6 now opens with the SDD as scaffolding and its two containment dimensions, and carries a Structural Rule listing the forbidden integration points plus the install-by-unpacking constraint that keeps the engine removable without editing a product manifest. The ambient surface is where this matters most — coupling arrives through a direct request ("add a lint script", "wire this into CI") rather than through a workflow the role cards mediate. The pre-commit hook is exempt in both places: `.git/hooks/` is neither committed nor packaged, and gating a commit is not gating a build.
- [C-001] fired as expected on the `rules/` edit and was repaired by re-linking and verifying by hash; a duplicate heading left by the retitling was caught on read-back, which is what the post-edit verification step is for.
- Engine 2.1.63 → 2.1.64 (C14, 66 files). Validation: harness 43/43 — including the shipped-text assertions that read both rewritten files as the implementation of a cognitive contract, the one way a prose-only phase can break a test; `validate-hardlinks` clean; `update-engine-meta --check` no drift.

## Phase 15 — 2026-08-06 (Finalize-Pipeline Accuracy & Generator Containment)

- Implemented the eight `Required Fix` blocks that had accumulated in [l2-engine-finalization.md](specifications/l2-engine-finalization.md) §7-§10 across seven consecutive spec-authoring cycles with zero implementation passes — the concept/implementation debt gap [l1-engine-core.md](specifications/l1-engine-core.md) 1.2.0 recorded. All eight were re-verified present in engine 2.1.62 before work started; none had been closed incidentally by the intervening phases.
- `update-state.js`: the phase-level progress counter now falls back to `tasks/phase-{N}.md` when `TASKS.md` carries no inline checklist heading, so it works on the canonical two-level layout instead of only the legacy one — this repository's own `STATE.md` had carried an aggregate-only Progress block for its entire history, a silent instance of the same gap. The merge classifier narrowed from any counter-shaped label to the two the engine actually emits, so hand-authored lines such as `Implementation: [1/5] in progress` survive a recompute instead of being classified as engine-owned and deleted. The Progress fence rewrite moved to a function-form replacement: a literal `$1` in operator narrative previously spliced a captured fence fragment into the middle of the note and left the document's triple-backtick count unbalanced — structural corruption, not merely a wrong value.
- `update-state.js` line-cap guard: it now reports "cap exceeded and nothing left to prune" distinctly from a successful prune. Its only pruning target is `## Recent Decisions`, which has a floor of one entry, while `## Blocking Constraints` grows monotonically by design; past the floor the file grew unbounded on every call while the warning kept claiming a prune had happened. Mirror-pruning the constraints was rejected: a Decision is disposable narrative, a Constraint is safety-critical.
- `finalize.js`: the Next Action computation no longer dispatches into a phase recorded as Blocked. Either signal — the phase file's frontmatter `status:` or its `TASKS.md` registry row — is sufficient on its own, since the two are written by different steps and are not atomic. Previously a Blocked phase with an open checklist line (Blocked is not Done) produced an execute-me recommendation contradicting the same file's `Status` and `## Blockers` fields, dispatching a resuming session straight into the recorded blocker.
- `finalize.js` + `commit-suggester.js`: the suggested commit body and the stdout artifact listing now enumerate the whole working tree, while the message header stays derived from the significance-whitelisted subset. The two had been collapsed onto one file set, so a task-execution finalize hid the task's own source deliverable — outside the whitelist by construction. Confirmed live during this phase: four engine files the pre-fix output would have omitted appeared in both listings.
- `commit-suggester.js`: the single-spec CHANGELOG bullet no longer interpolates a specification's derived identifier into the product's root `CHANGELOG.md`. This generator is the one surface the write-time and review-time containment gates cannot reach — nothing here is authored by a role or reviewed as a diff — so its enforcement is regression coverage, now in place for this branch and the two adjacent ones.
- `run.md` §2.5: the per-task state update no longer passes `--status=`. That flag targets the phase-level field, whose vocabulary is `Active | Blocked | Paused`; the documented per-task form wrote `Done` into it after one task of a multi-task phase finished. The phase-start and Pause Propagation call sites are unchanged — the latter is phase-scoped by construction.
- Harness 34 → 43 tests. One deliberate deviation from the spec, caught by existing coverage: §8.4's literal replacement regex omits the state template's placeholder label form (`Phase {N}`), so applying it verbatim demoted a bootstrap placeholder line to narrative and left it in place. Implemented with the placeholder alternation; flagged in `PLAN.md` for a spec amendment rather than fixed there, since spec authoring is a different workflow's write scope.
- Engine 2.1.62 → 2.1.63 (C14, 66 files). Validation: harness 43/43; `update-engine-meta --check` clean; `check-prerequisites` ok with an empty warnings array, closing the `SYNC_GAP` this cycle opened with.

## Phase 14 — 2026-08-06 (Shipped Reference Hygiene & Documentation Sync)

- Fixed the skill-projection normalizer in `sync-skills.js`, which destroyed the `.md` extension of any `rules/magic.md` reference passing through it. Root cause was scope, not the regex: the extension guard existed in the frontmatter path only, while the body path ran an unguarded chain — and *both* paths shared a slash-prefixed pattern that matches `/magic.md` inside `rules/magic.md`, so even the "guarded" path would have mangled a path-form reference had one appeared in frontmatter. The two chains are now one `normalizeMagicReferences()` helper with a single `hyphenateMagicToken()` rule applied to both replacements. Field evidence: `skills/magic-run/SKILL.md` and `skills/magic-task/SKILL.md` shipped `rules/MAGIC-md` — a target broken in two independent ways.
- Purged the `rules/MAGIC.md` case drift from every shipped artifact: `.magic/analyze.md` (2, one of them a live markdown link `[...](../rules/MAGIC.md)` that dangles on any case-sensitive filesystem), `.magic/run.md` (2), `workflows/magic.run.md`, `workflows/magic.task.md`, `README.md`. The engine repo runs on NTFS, so the whole class was invisible locally while broken for every Linux and macOS installation. A prior changelog entry records repairing "a dangling `rules/MAGIC.md` reference" — these were the branches that repair did not reach.
- Confirmed and repaired **[C-001]** in practice: editing the two wrappers delinked their `.agents/workflows/` twins, which then held the pre-edit content. Links recreated and verified by hash, not by editor exit code.
- Documentation sync: README engine reference 2.1.55 → 2.1.62; CONTRIBUTING workspace row 25 → 27 specs with its dead `engine/INDEX.md` link repaired; `docs/README.md` lost its never-authored `./workspaces.md` link in favour of an inline `workspace.json` example, and gained the missing Status and Graph rows; authored `docs/graph.md` and `docs/status.md`, which had shipped without user documentation since their introducing phases.
- SDD-layer backfill: `l2-multi-angle-review.md` 1.0.0 → 1.0.1 gained the `## Canonical References` section it was the only Stable spec to lack; `l1-engine-core.md` 1.1.3 → 1.1.4 now cites C6 and C10, the two conventions the graph classified as orphaned — implemented in the planning workflow but traceable from no specification. Convention coverage is now `Orphaned: 0`.
- Engine 2.1.61 → 2.1.62. Validation: harness 34/34; `update-engine-meta --check` clean; zero `MAGIC.md` occurrences across `.magic/`, `workflows/`, `skills/`, `rules/`, `docs/`, `README.md`, `CONTRIBUTING.md`; 239 relative links across `.design/`, `docs/`, `rules/`, `workflows/` and the root docs — 0 broken.
- Note on the link sweep: its first pass reported one false positive, a quoted `../rules/MAGIC.md` inside a code span in the phase file. Code spans are stripped before extraction — a documented defect is not a broken link.

## Phase 13 — 2026-06-13 (Upgrade-Detection Decision-Autonomy Alignment)

- Aligned `rules/MAGIC.md` §1 Engine Upgrade Detection with the Decision Autonomy protocol (`l1-decision-autonomy.md` v1.2.0 §5.3c): the `[y/n]` prompt and `On y` / `On n` branches are replaced with a single informational line that narrates the drift, recommends `/magic.analyze`, and proceeds with the requested workflow — never a blocking prompt (DA-8 one-path, DA-9 declarative; mirrors the `/magic.status` SC-4 treatment). The snapshot-update-only-via-analyze contract and the analyze/status exemptions + `MAGIC_DRIFT_CHECK=0` are preserved.
- Updated the README Updating section: removed "asks **[y/n]**" / "will prompt you"; it now describes surfacing the drift and recommending `/magic.analyze` (no blocking prompt).
- Recreated the `.agents/rules/magic.md` hardlink after the `rules/` edit (C-001); `validate-hardlinks` confirms the rules group is linked.
- Engine version unchanged (2.1.41): `update-engine-meta` change-detection and `.magic/.checksums` cover `.magic/` only, so this `rules/`-only edit does not bump the version. Validation: harness 15/15; residual `[y/n]` grep clean (only the prohibition line remains); `check-prerequisites` ok.
- Findings surfaced (backlog): **R8** — `rules/` and `skills/` (engine L1) are outside C14 version/checksum tracking, so L1-only changes can ship without a version bump; **R9** — the root AGENTS-family hardlinks (GEMINI/QWEN/CODEX) are delinked (`nlink=2`), restore via `/magic.dev.init`.
- Field evidence: §1 was the recurring drift friction resolved via `[DR]` on every `/magic.spec` invocation this session — the DA-9 deployment tail that Phase 9 (proposal-surface alignment) did not reach.

## Phase 12 — 2026-06-13 (Wrapper-Body Parity Check — R4)

- Deployed the `WRAPPER_BODY_DRIFT` check into `magic.analyze` Mode C (`l2-workflow-wrappers.md` v1.2.0 §6): the Design Registry Audit now verifies that every `workflows/magic.{cmd}.md` carrying a "Full implementation" pointer has its `.magic/{cmd}.md` body on disk. Self-contained wrappers (no pointer, e.g. `magic.graph.md`) are skipped; engine bodies without a wrapper (`context.md`, `init.md`, `pause.md`, `retrospective.md`) are allowed — the invariant is one-directional.
- Added a `Wrapper Drift` row to the Advisory Report Findings Schema and a parity-check line to both Mode C checklists (Ventilation steps + Task Completion).
- Cognitive check (no new script) — consistent with the existing Mode C structural checks (Ghost/Zombie, Case Sensitivity, Link Integrity); the Mode C checklist is the enforcement surface.
- Engine version 2.1.40 → 2.1.41 (C14); checksums regenerated (65 files).
- Validation: harness 15/15 (no new test — cognitive); `update-engine-meta --check` no drift; cognitive dry-run over the current 7 wrappers reports zero `WRAPPER_BODY_DRIFT` (tree parity-clean, check classifies correctly).
- Preventive: this is the first non-bugfix engine improvement of the session — it closes the silent-drift class behind the phantom `magic.graph → .magic/graph.md` mapping that survived 13 registry versions.

## Phase 11 — 2026-06-13 (Archiver Eligibility Fix — R7)

- Fixed `phase-archiver.allChecked` (`scripts/lib/phase-archiver.js`) per `l2-engine-finalization.md` v1.2.0 §6: it now strips fenced code blocks and inline code-spans, then tests a line-anchored checklist pattern (`/^\s*- \[ \]/m`). Previously a whole-file `content.includes('- [ ]')` substring scan false-positived on any phase whose prose/Notes mentioned checkbox syntax, silently suppressing archival.
- Added an archiver-eligibility regression test to `dev/tests/engine.js` via the public `findArchiveCandidates` export: a phase with `- [ ]` quoted in Notes but all checklist items checked is archivable; a phase with a genuine unchecked checklist line is not. Harness 14 → 15 tests, all green.
- Re-archived `phase-10.md` — the phase R7 had blocked — via `archive-phases`; it moved to `archives/tasks/` and its TASKS.md row is now `Done (Archived)`.
- Engine version 2.1.39 → 2.1.40 (C14); checksums regenerated (65 files). `dev/tests/engine.js` is L2 (dev/) — the bump is from `phase-archiver.js`.
- Validation: harness 15/15; `update-engine-meta --check` no drift; `check-prerequisites` clean; hardlinks linked.
- Field evidence: phase-10's own task Notes (which discuss `- [ ]` detection for SC-2.1) tripped the old substring scan — the defect surfaced by the immediately preceding phase's content.

## Phase 10 — 2026-06-13 (Session-Continuity Hardening Deployment)

- Deployed SC-2.1 (plan-state-aware `Next Action`, `l1-session-continuity.md` v1.1.0): `computeNextAction` in `scripts/finalize.js` now derives the recommendation from the actual plan ledger — open `- [ ]` tasks → `/magic.run`; plan complete → `/magic.spec` (new scope); spec/rule → `/magic.task` (replan); unreadable ledger → `/magic.task` fallback. The static `task: "execute the active phase"` map entry — which misdirected the returning user whenever the plan was complete — is removed.
- Made `finalize.js` requirable for unit testing: `module.exports = { main, computeNextAction, updateSessionState }` behind an `if (require.main === module)` CLI guard (same pattern as `update-state.js`).
- Added the first finalize-pipeline regression coverage to `dev/tests/engine.js` (`l2-test-suite.md` v1.5.0 mandate): a `computeNextAction` unit test across all SC-2.1 branches (asserting the plan-complete branch never says "execute the active phase"), and an end-to-end skip-path test asserting SC-2 STATE.md patching, the SC-2.1 `/magic.spec` next-action, and the SC-3 non-bumping commit suggestion with no version bump. Harness 12 → 14 tests, all green.
- Engine version 2.1.38 → 2.1.39 (C14); checksums regenerated (65 files). `dev/tests/engine.js` is L2 (dev/) — no engine bump from the test edit; the bump is from `finalize.js`.
- Validation: harness 14/14; `update-engine-meta --check` no drift; `check-prerequisites` clean; hardlinks linked.
- Field evidence: `computeNextAction` produced a stale "execute the active phase" recommendation across this session's plan-complete states (hand-corrected in STATE.md three times) — the defect that motivated SC-2.1.

## Phase 9 — 2026-06-13 (DA-9 Engine Deployment — Proposal Surfaces)

- Deployed DA-9 (Proposal Surfaces Are Declarative, from `l1-decision-autonomy.md` v1.1.0) into the engine workflow bodies, closing the gap that let a proposal step render as a user-facing question.
- `spec.md`: Blank Trigger (Creative Sparks) rewritten from the pre-C27 "propose … in the next turn … auto-pick" wait-form to same-turn DA-3 selection narrated as a `[DR]`; Mode Transition and Dispatch Notice gained explicit DA-9 cross-references. Firing gates left intact (WI-4 three-option / E5; T1-T3 "Propose & Wait" / E4).
- `analyze.md`: post-dispatch Zero-Prompt Handoff rewritten — removed "Proceed to Plan/Run? and wait for reply"; now narrates `[DR] Specs Stable — invoking /magic.task` and pauses only on a firing E3 hard-fork (DA-5 single question).
- `task.md`: verified already DA-9-compliant (Auto-Plan "no Go confirm; no menu"); added a one-line DA-9 cross-reference so deployment coverage is audit-greppable across all three bodies.
- Engine version 2.1.37 → 2.1.38 (C14); checksums regenerated (65 files); skill wrappers re-projected.
- Validation: engine test harness 12/12 green; residual proposal-as-question grep clean across the three bodies (sole match is the negation phrase in `analyze.md`); firing gates verified present; `update-engine-meta --check` no drift; hardlinks linked.
- Field evidence: a live non-whitelisted `AskUserQuestion` in a `/magic.spec` Blank Trigger (this session) — the violation that motivated DA-9 and this deployment.

## Phase 8 — 2026-06-12 (Session Continuity & Status Command)

- Wired the SC-2 state-update step into `scripts/finalize.js`: STATE.md is patched (Updated timestamp, pipeline-order Next Action, auto-progress) on BOTH the significant and the skip path of every `finalize --workflow=<spec|task|run|rule>` run; non-blocking on failure; `--dry-run` previews the patch; output gains a `STATE.md` status row.
- Refactored `scripts/update-state.js`: CLI moved behind a `require.main` guard, `updateState`/`computeProgress` exported, new `--auto-progress` flag recomputes the STATE.md Progress block (active-phase checklist + overall phase registry) from TASKS.md.
- Implemented the SC-3 non-bumping commit-suggestion fallback: significance miss + dirty working tree → exactly one suggested Conventional Commits message labeled `(non-bumping)` with a neutral `chore` header (new `type`/`summary` overrides in `lib/commit-suggester.js`); no version bump, no CHANGELOG entry; write-side git remains forbidden.
- Created the `/magic.status` read-only resume briefing: `.magic/status.md` body (5 invariants, seven-section briefing, degraded states), `workflows/magic.status.md` thin wrapper, `skills/magic-status/SKILL.md` generated via skill sync — parity verified.
- Registered status in the upgrade-detection exemptions of `rules/magic.md` §1 (drift becomes an informational engine line, never a prompt); `.agents/rules/magic.md` hardlink recreated and validated after the edit.
- README command table: added `/magic.status` and `/magic.graph` (pre-existing gap).
- Engine version 2.1.34 → 2.1.37 (C14 ×3); checksums 64 → 65 files; skill wrappers re-projected.
- Validation: engine test harness 12/12 green; check-prerequisites clean (0 warnings); briefing dry-run renders all seven sections with a correct informational drift line.

## Phase 5 — 2026-06-12 (Decision Autonomy)

- Amended `templates/rules.md`: C13 §3 rewritten from halt-and-ask "Zero Assumptions" to "Bounded Ambiguity Resolution" (resolve via C27, halt only at the Escalation Whitelist); appended the C27 section (DA-1..DA-8 operational summary, RC-9-compliant wording).
- Added §7 "Autonomous Decision Protocol (C27 Session Posture)" to user-side `rules/magic.md`: DA-6 session persistence (next step narrated, never asked), DA-4 Decision Record grammar, whitelist summary; Completion Protocol renumbered §7→§8 with a new §7 checklist item; hardlink recreated and validated.
- Wired `Decision Autonomy (C27)` line into the completion checklists of `spec.md`, `task.md`, and `run.md`.
- Bound the C27 anti-pattern line ("Elective questions outside the C27 escalation whitelist (E1-E5) are a protocol violation.") into `templates/role.md` and all 14 role cards.
- Engine version 2.1.33 → 2.1.34 (C14); checksums regenerated (64 files); skill wrappers re-projected.
- Validation: session-evidence simulation — [DR] resolution at elective forks, single-question format at the whitelist gate — both assertions PASS; engine test harness 12/12 green.

## Phase 4 — 2026-06-12 (Prompt Quality Gate)

- Created `.magic/roles/prompt-engineer.md` — role card #14 (reviewer): five workflow triggers, six-dimension PQ-3 protocol, PQ-6 verdict semantics (PASS / PASS-WITH-REWRITES / FAIL).
- Wired five gates into workflow bodies: `spec.md` Post-Update Review gains the Instruction Quality Pass (second stage after spec-critic PASS); `task.md` gains step 5a Task Instruction Review (after planner audit, before write-back); `rule.md` gains §5a Rule Wording Review (after APPROVE, before write); `run.md` gains conditional Step 3.4b Instruction Diff Review (fires only on diffs touching AI-facing instruction artifacts); `analyze.md` Mode C gains step 7 Prompt Quality Audit (advisory sweep feeding the pre-advisory pool; steps renumbered 8-15).
- Extended `templates/rules.md` §C24 pointer table: 4 mandatory prompt-engineer gate rows + opt-in 3.4b note; registry count 13 → 14.
- Engine version 2.1.32 → 2.1.33 (C14); checksums 63 → 64 files; skill wrappers re-projected.
- Validation: role registry integrity 14/14 (0 dormant, 0 missing, 0 dangling); engine test harness 12/12 green. Step 3.4b self-applied to this phase's own diff — verdict PASS.

## Phase 7 — 2026-06-12

- Purged all 15 references to the engine repository's own SDD workspace from shipped engine files per RC-9 (l1-sdd-reference-containment.md v1.1.0), in three classes:
  - **Dead spec links (5)**: `rule.md`, `analyze.md` (×2), `spec.md`, `task.md` — markdown links into `.design/engine/specifications/` replaced with the invalidation rule stated inline.
  - **Baked-in workspace name (3)**: `run.md`, `spec.md`, `rules/magic.md` — phase-journal path generalized to `.design/{ws}/CHANGELOG.md` (consumers do not have an `engine` workspace).
  - **Governance citations (6)**: `context.md`, `init.md` (×2), `spec.md`, `templates/rules.md` (×2) — spec file names replaced with protocol names; WI-labels preserved as stable in-text identifiers.
- Annotated Phase 5 tasks (constitution-template mirroring) with an RC-9 guard so the leak class is not reintroduced.
- Engine version 2.1.31 → 2.1.32 (C14); checksums regenerated, skill wrappers re-projected; `.agents/rules/magic.md` hardlink recreated after the rules edit.
- Validation: containment greps 0/0/0 across `.magic/`, `workflows/`, `skills/`, `rules/` (sole residual hit is the illustrative BAD example, exempt); engine test harness 12/12 green.

## Phase 6 — 2026-06-12

- Added §6 "SDD Reference Containment" to user-side `rules/magic.md`: one-way traceability rule (SDD artifacts reference code, never the reverse), forbidden reference classes, exemptions, BAD/GOOD example, enforcement map; Completion Protocol renumbered §6→§7 with a new containment checklist line; recreated the broken `.agents/rules/magic.md` hardlink.
- Coder card (`.magic/roles/coder.md`): containment authoring gate inserted as protocol step 6 + anti-pattern — no SDD-artifact references in code, comments, docstrings, identifiers, string literals, or test names.
- Code-reviewer card (`.magic/roles/code-reviewer.md`): containment check inserted as protocol step 4 + anti-pattern — any SDD-layer reference in a product-file diff is FAIL; internal minimalism cross-reference renumbered.
- Ventilation (`.magic/analyze.md`): Mode C gained step 6 "SDD Reference Containment Scan" emitting advisory `SDD_REFERENCE_LEAK {file}:{line}` findings; "SDD Leak" row added to the Findings Schema; both Mode C checklists extended.
- Engine version 2.1.30 → 2.1.31 (C14): checksums regenerated (63 files), skill wrappers re-projected.
- Validation: containment-gate simulation passed (authoring forbid + review FAIL + repo scan clean outside exempt zones); engine test harness 12/12 green.
- Follow-up surfaced (not fixed, out of phase scope): shipped `analyze.md`/`spec.md`/`task.md` link to `.design/engine/specifications/l2-spec-graph-memory.md` — dead on consumer installs; same violation class, needs its own task.

## Phase 4 — 2026-05-12

- Integrated coding discipline from the local reference into `.magic/roles/`: Coder now surfaces material assumptions and writes minimal diffs; Code-reviewer enforces traceability; Code-simplifier rejects speculative complexity; Code-skeptic classifies material assumptions; Test-engineer requires explicit `Verify` evidence.
- Added mandatory `Verify` criteria to `.magic/task.md` decomposition and `.magic/templates/phase.md` task blocks.
- Added explicit `Verify Criterion` guard to `.magic/run.md` QA Review so the execution workflow mirrors the role-card requirement directly.
- Updated engine specs `l2-role-cards.md`, `l2-role-integration.md`, and `l2-engine-templates.md`; registry versions synchronized in `.design/engine/INDEX.md`.

## Phase 3 — 2026-04-23

- Created `.magic/roles/` directory with 13 role cards: orchestrator, planner, coder, code-reviewer, code-simplifier, code-skeptic, test-engineer, debugger, docs-specialist, spec-critic, project-auditor, constitutional-reviewer, retrospective-analyst
- Created `.magic/templates/role.md` authoring template with YAML frontmatter schema
- Amended `.magic/run.md`: rewrote Execution Setup table (Manager→Orchestrator, Developer→Track Owner Context), added gates 3.3/3.4/3.6, `@role:coder`, `@role:code-reviewer`, `@role:code-simplifier`, `@role:code-skeptic`, `@role:debugger`, `@role:docs-specialist`, `@role:test-engineer`, updated Run Completion Checklist; fixed "Developer track / Manager role" → "Track Owner / @role:orchestrator"
- Amended `.magic/task.md`: "Planning Skeptic persona" → `@role:planner`; updated Task Completion Checklist
- Amended `.magic/spec.md`: "Project Critic persona" → `@role:spec-critic`; updated Task Completion Checklist
- Amended `.magic/analyze.md`: "Auditor persona" → `@role:project-auditor`; updated Pre-Advisory Audit heading and both checklist lines
- Amended `.magic/rule.md`: "Constitutional Reviewer persona" → `@role:constitutional-reviewer`; updated section header and Post-Write checklist
- Amended `.magic/retrospective.md`: "Independent Analyst persona" → `@role:retrospective-analyst`; updated section header and Retro Checklist
- Rewrote `.magic/templates/rules.md` §C24 as role-registry pointer-table; bumped template to v1.3.0
- Extended `.magic/scripts/check-prerequisites.js` with `role_registry_integrity` block (ROLE_MISSING/ROLE_HANDOFF_DANGLING HALT; ROLE_DORMANT/ROLE_TRIGGER_UNRESOLVED WARN)
- Extended `.magic/scripts/update-engine-meta.js` with role-card history collapsing (`roles/*` → `roles` category)
- Engine version bumped: 1.5.182 → 1.5.183; all 13 role cards registered in `.checksums`
- `check-prerequisites` validation: `role_registry.total=13`, `missing=[]`, `dangling_handoffs=[]` ✓

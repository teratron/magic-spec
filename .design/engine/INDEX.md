# Engine Specifications Registry

**Version:** 1.29.1
**Status:** Active

## Overview

Central registry for the Magic SDD core engine and its automated subsystems.

## System Files

- [../RULES.md](../RULES.md) - Global project constitution.

## Domain Specifications

| File | Description | Status | Layer | Version |
| --- | --- | --- | --- | --- |
| [l1-engine-core.md](specifications/l1-engine-core.md) | Core logic, workflows, invariants (incl. C6/C10 traceability bindings), runtime guards, and Known Process Gaps (concept/implementation debt asymmetry — proposed debt-ceiling convention rejected by explicit user ratification, non-blocking `concept-only` classification targeting `.magic/analyze.md`'s Bare-L1-without-L2 advisory as an alternate resolution; workspace scope completeness — `dev` added to `engine`'s scope, resolved; Mode C Depth Control bypass ambiguity in `analyze.md`, resolved; dev-repo Engine-Version snapshot sync — `.design/INDEX.md` patched atomically with C14 in this repo only, consumer projects unchanged; fresh-project snapshot ambiguity — Engine Upgrade Detection no longer narrates a drift recommendation on a project with no prior `.design/INDEX.md`, resolved). | Stable | 1 | 1.8.0 |
| [l2-engine-automation.md](specifications/l2-engine-automation.md) | Automation scripts, history subsystem, execution logic, coverage denominator scope (EXEMPT classification for SDD bookkeeping files incl. INDEX.md/RULES.md, §Coverage Denominator Scope), the DESIGN_DEBT_PENDING plan-complete structural predicate Required Fix, its Zero-Row Vacancy amendment, and the user-installation boundary of the C14 write branch, and diagnosable integrity drift — how a flagged file differs, not only that it does (§Engine Meta Update Flow). | Stable | 2 | 1.16.0 |
| [l2-engine-finalization.md](specifications/l2-engine-finalization.md) | Finalization pipeline contract (`scripts/lib/`): module inventory, invocation surface, opt-out, session-continuity step §5 (SC-2; SC-3 retired 2026-08-27, write-side git prohibition retired 2026-09-17), archival eligibility §6 (C8; §6.1 Workbook Name Recognition — canonical `phase-{N}[{track}].md` shape, single shared recognizer module), archival index rewrite §7 (self-labelling PLAN.md links), terminal block ownership §8 (DG-5/DG-6; no longer carries a commit-suggestion notice). Decomposed at v2.0.0 — defect registers extracted to the two child specs below; §5.1 notes the checkpoint claim owned by l2-session-checkpoint.md. | Stable | 2 | 3.2.2 |
| [l2-finalize-state-accuracy.md](specifications/l2-finalize-state-accuracy.md) | Child of l2-engine-finalization: the register of record for every SC-1/SC-2 defect in the STATE.md update step — seventeen, all implemented, under permanent defect numbers (a Defect Register maps each to its file, so every `§N` cited from engine code, the harness and other specs keeps resolving) — and owner of the caller-side defects: the Blocked-phase next action (§2), the Status field collision (§4), task-level Blocked/Assignment precedence (§9) and `finalize.js`'s own workspace-scoping defect (§11 — `MAGIC_DESIGN_DIR` precedence inversion). Decomposed at 2.0.0 (552 lines against the 500-line SPEC_DECOMPOSE threshold); the `update-state.js` writer's own defects live in the two children below. | Stable | 2 | 2.0.0 |
| [l2-update-state-structure.md](specifications/l2-update-state-structure.md) | Child of l2-finalize-state-accuracy: how `update-state.js` locates, groups and creates what it writes — the decision-section structural defect and its sibling call site (§8, §8.5), multi-line entry truncation and its reopening in the scalar-field loop (§12, §12.1), and the missing-section silent drop with its two reopenings in the field/`## Progress` writers and the line-cap guard (§13, §13.1, §13.2). Seven defects, all implemented; extracted verbatim at the register's 2.0.0 decomposition. | Stable | 2 | 1.0.0 |
| [l2-update-state-values.md](specifications/l2-update-state-values.md) | Child of l2-finalize-state-accuracy: what `update-state.js` computes and bounds — progress granularity (§3), progress over-classification (§5), replacement-string injection in the `## Progress` rewrite and its reopening in the scalar-field loop (§6, §6.1), line-cap exhaustion (§7) and the Recent-Decisions archival-promise gap (§10). Six defects, all implemented; extracted verbatim at the register's 2.0.0 decomposition. | Stable | 2 | 1.0.0 |
| [l2-finalize-output-contract.md](specifications/l2-finalize-output-contract.md) | Child of l2-engine-finalization: the emitted-artifact surface — RC-11 generator containment, stdout-listing completeness (formerly SC-3.1; commit-message clause retired 2026-08-27), and CHANGELOG entry suppression (§4.3 root-caused to two 2026-05 manual edits; §4.4 fix — explicit opt-in `release-changelog` subcommand, never an automatic finalize side effect; §4.5 — field-confirmed discoverability gap, finalize's deduped-CHANGELOG row must hint at `release-changelog`). | Stable | 2 | 2.0.0 |
| [l2-test-suite.md](specifications/l2-test-suite.md) | Testing architecture, cognitive suite (211 tests, v1.9.76, incl. T209–T212 Idea Intake Gate) + script-level harness with finalize-pipeline coverage mandate (C11 simulation, SC-2.2 matrix sweep, RC-2.1 shipped-text contract, RC-11 generator containment, SC-1.1/SC-2.1(a)/SC-2.3 STATE.md accuracy cases incl. replacement-string injection, non-whitelisted file visibility (stdout only — SC-3.1's commit-message case retired), SC-1.2 line-cap guard exhaustion, DG-1..DG-9 diagnostics digest incl. cross-path output ordering, 72 tests incl. negative-controlled update-engine-meta.js regeneration-trigger and validate-hardlinks.js §19 pair coverage), plus the session-checkpoint coverage mandate for SC-1.3 and SC-6..SC-9 (case list in l2-session-checkpoint.md §6). | Stable | 2 | 1.18.0 |
| [l1-documentation-system.md](specifications/l1-documentation-system.md) | Knowledge base structure, governance, and docs/ sync policy. | Stable | 1 | 1.2.2 |
| [l1-config-drift-guard.md](specifications/l1-config-drift-guard.md) | Detection of manual RULES.md changes outside workflow lifecycle. | Stable | 1 | 1.0.1 |
| [l2-workflow-wrappers.md](specifications/l2-workflow-wrappers.md) | User-facing workflow entry points in `workflows/`; wrapper-body parity invariant + WRAPPER_BODY_DRIFT check (§6), and its converse: internal modules are never advertised as commands, checked by PHANTOM_COMMAND. | Stable | 2 | 1.3.0 |
| [l2-skill-wrappers.md](specifications/l2-skill-wrappers.md) | Deployment-facing skill entry points in `skills/`, incl. §3.2 the regeneration-trigger invariant — runs on every write bound to `workflows/`, never gated on the unrelated `.magic/` checksum manifest. | Stable | 2 | 1.4.0 |
| [l2-engine-templates.md](specifications/l2-engine-templates.md) | Structural blueprints for specs, plans, tasks, and phases. | Stable | 2 | 1.1.0 |
| [l2-agent-surface.md](specifications/l2-agent-surface.md) | Adapter-facing surface definition (`.agents/`), incl. §4 the closed linked-pair inventory (AGENTS family, `rules/`, `workflows/`) and the explicit `skills/` non-pair exclusion. | Stable | 2 | 1.1.0 |
| [l1-role-system.md](specifications/l1-role-system.md) | Unified role registry; supersedes scattered C24 persona mentions and adds code-writing stage roles. | Stable | 1 | 1.1.0 |
| [l1-prompt-quality-gate.md](specifications/l1-prompt-quality-gate.md) | Instruction-quality governance for AI-facing artifacts: PQ taxonomy, verdict semantics, universal gate policy. | Stable | 1 | 1.0.0 |
| [l2-role-cards.md](specifications/l2-role-cards.md) | Role card registry — file format, frontmatter schema, and 14-card inventory index. | Stable | 2 | 2.1.0 |
| [l2-role-cards-execution.md](specifications/l2-role-cards-execution.md) | Execution-pipeline card content (planner, orchestrator, coder, debugger, docs-specialist); Coder authoring gate carries RC-2.1 bare-form scan. | Stable | 2 | 1.2.0 |
| [l2-role-cards-review.md](specifications/l2-role-cards-review.md) | run.md review-gate card content (code-reviewer, code-simplifier, code-skeptic, test-engineer); RC-6 check bound to RC-2.1 patterns. | Stable | 2 | 1.2.0 |
| [l2-role-cards-governance.md](specifications/l2-role-cards-governance.md) | Governance card content (spec-critic, project-auditor, constitutional-reviewer, retrospective-analyst, prompt-engineer incl. the conditional Idea Intake Gate audit, E6). | Stable | 2 | 1.2.0 |
| [l2-role-integration.md](specifications/l2-role-integration.md) | Workflow-body integration of the role system: run/task/spec/analyze/rule/retro amendments + §C24 rewrite. | Stable | 2 | 2.1.0 |
| [l2-role-tooling.md](specifications/l2-role-tooling.md) | Role system engine tooling: check-prerequisites role_registry_integrity, update-engine-meta treatment, role template. | Stable | 2 | 1.0.0 |
| [l2-spec-graph-memory.md](specifications/l2-spec-graph-memory.md) | Spec Graph memory & token economy: extraction cache, wiki export, MCP token-budget, workflow integration triggers. | Stable | 2 | 1.1.2 |
| [l1-workspace-intent-routing.md](specifications/l1-workspace-intent-routing.md) | Pre-Resolution intent detection, ambiguity gate, atomic workspace creation, fit validation; WI-10 documentation-parity scope broadened to every init.md surface; simulation matrix registered as Canonical Reference. | Stable | 1 | 1.1.1 |
| [l1-decision-autonomy.md](specifications/l1-decision-autonomy.md) | Autonomous Decision Protocol: escalation whitelist, deterministic selection, Decision Records, single-question format, declarative proposal & drift-revalidation surfaces (DA-9), idea-intake gate as whitelist entry E6 with its DA-1 containment note. | Stable | 1 | 1.3.0 |
| [l1-sdd-reference-containment.md](specifications/l1-sdd-reference-containment.md) | SDD↔product boundary in two dimensions: reference containment (product files never reference SDD artifacts — enforcement gates, notation-independent detection, remediation owner, shipped and generator self-containment) plus structural containment (RC-12 scaffold boundary + removal test, §4.4 check). RC-1..RC-12. Plus a provisional-boundary assumption (§2): the scaffold relationship is temporary by design, anticipating an eventual consumer-project self-hosting handoff. | Stable | 1 | 1.5.0 |
| [l1-session-continuity.md](specifications/l1-session-continuity.md) | Session continuity: STATE.md live-memory contract (Status field scope SC-1.1, line-cap enforcement SC-1.2), universal post-command state updates (plan-state-aware next action SC-2.1 incl. Blocked-phase precedence SC-2.1(a), task-level Blocked/Assignment precedence SC-2.1(c), provenance-free field SC-2.2, progress granularity SC-2.3, structural-corruption is an SC-2 defect), status briefing surface (SC-1, SC-2, SC-4, SC-5), plan-complete disambiguation (SC-2.4) + Backlog Disposition Convention, Terminal-Row Recognition, and its Zero-Row Vacancy addenda (both the archived-row case and the vacant-section case). Commit suggestion guarantee (SC-3, incl. message completeness SC-3.1) retired 2026-08-27; the write-side git prohibition that survived it retired 2026-09-17 — both by explicit user directive. Automatic checkpoints (2.3.0): Cold-Start Sufficiency SC-6 (checkpoint claim SC-6.1), Observable Triggers SC-7, Dead-End Record SC-8, Resume From Recorded State SC-9, No Dead Fields SC-1.3, plus a carrier map of the six-section handoff practice (§4.4). | Stable | 1 | 2.3.0 |
| [l2-session-checkpoint.md](specifications/l2-session-checkpoint.md) | Session checkpoint contract: the deliverables of the automatic-checkpoint invariants — the task-start record, the `Attempts` dead-end field, one shared read-only resume script (`resume-state`) and its call sites, the checkpoint claim in finalize output, retirement of the fill-percentage triggers (post-compaction re-grounding instead), removal of the dead `Last Session Ended` field and the phantom `/magic.pause` command, regression coverage H1–H10 and C1–C5, and the thirteen-row deployment inventory. | Stable | 2 | 1.0.1 |
| [l2-status-command.md](specifications/l2-status-command.md) | Read-only `/magic.status` briefing command: layout contract, degraded states (the in-flight and paused resume line comes from the shared resume predicate), wrapper/skill surface (C2 exception). | Stable | 2 | 1.2.0 |
| [l2-release-pipeline.md](specifications/l2-release-pipeline.md) | GitHub Release CI: tag-driven build of the L1 archive, version source, publication/retry, engine-version tracking constraint (R8), adapter distribution reference (C17). | Stable | 2 | 1.0.1 |
| [l1-multi-angle-review.md](specifications/l1-multi-angle-review.md) | Multi-Angle Review & Decision Synthesis Protocol: 5 evaluation lenses, blind cross-evaluation, context auto-enrichment, and structured synthesis. | Stable | 1 | 1.0.0 |
| [l2-multi-angle-review.md](specifications/l2-multi-angle-review.md) | Multi-Angle Review & Decision Synthesis Implementation: prompt mappings, blind cross-review workflow, and role integrations. | Stable | 2 | 1.0.1 |
| [l1-scan-input-hygiene.md](specifications/l1-scan-input-hygiene.md) | Text-scan input preparation: mention/use boundary (SH-1), strip-before-match ordering (SH-2), template sources exempt from resolution (SH-3), bounded token capture (SH-4), one shared strip implementation (SH-5). Enforcement-surface table with per-scan compliance state — 3 of 4 `check-prerequisites.js` sites still open. | Stable | 1 | 1.2.0 |
| [l1-engine-diagnostics.md](specifications/l1-engine-diagnostics.md) | Engine diagnostics digest: non-fatal findings (errors that did not abort, warnings, engine-applied corrections) are recorded rather than only printed, aggregated across processes, and rendered once immediately before the next step. DG-1..DG-10, incl. severity taxonomy, finding shape, exactly-once drain, empty-state silence, the agent-side channel, and pre-render revalidation of condition findings. | Stable | 1 | 1.1.0 |
| [l2-engine-diagnostics.md](specifications/l2-engine-diagnostics.md) | Diagnostics implementation: `lib/diagnostics.js` collector, JSONL sink under `.design/.cache/`, `record-diagnostic` executor subcommand, single `emitTail()` in finalize (digest + next step only — no commit-suggestion notice as of 2026-08-27), `revalidate()` pre-render recheck of condition findings (DG-10), and the migration inventory of all 17 non-fatal emitters with assigned severities and codes. | Stable | 2 | 1.2.2 |
| [l1-idea-intake-gate.md](specifications/l1-idea-intake-gate.md) | Input-side quality gate: raw idea refined into specification-ready intent before dispatch. IK-1..IK-9 — self-resolution mandate, intent-only question domain (technical realization never asked), closed firing conditions F1 incoherence / F2 essence ambiguity, plain-language mandate, convergent unbounded dialogue with non-progress termination, chat-only residency, C27 whitelist entry E6, Anti-Stall reconciliation. Symmetric counterpart to `l1-prompt-quality-gate.md` (output side). | Stable | 1 | 1.0.0 |

## Meta Information

- **Maintainer**: Core Team
- **License**: MIT
- **Last Updated**: 2026-09-21 (No spec-count change, 1 amended, patch. Found while implementing Phase 32's `resume-state` script: `l2-session-checkpoint.md` 1.0.0 specified the all-workspace scope as the absence of `--workspace`, which no script behind `executor.js` can honor — the executor consumes the flag and substitutes the registry default when it is absent, handing the result on only as `MAGIC_DESIGN_DIR`, so a script cannot tell "omitted" from "defaulted". 1.0.1 makes the scope an explicit `--all` flag; the rule that a caller with a resolved workspace stays inside it (C15) is unchanged. Planning had assumed the flag reached the script. No status transition.)
- **Previous**: 2026-09-20d (Spec count 35 → 36: one added, five amended, one patched. User directive after an audit of session handoff: the user asked whether the engine already carries the widely recommended per-task handoff file, then ruled that no new command is wanted and everything must run under the hood. The answer was that `STATE.md`, the phase workbook and `HANDOFF.json` already carry five of the six sections, but the path meant to deliver the guarantee was unmeasurable (a fill-percentage trigger), unreachable (`/magic.pause` is not a command), unexercised (no snapshot ever committed, no coverage), self-contradicting on resume (a file-presence trigger over a file that must outlive resume) and blind to work in flight (details in `l1-session-continuity.md` §1.5, from a read of shipped text and repository history). `l1-session-continuity.md` 2.2.0 → 2.3.0: SC-6 Cold-Start Sufficiency (with SC-6.1 Checkpoint Claim), SC-7 Observable Triggers, SC-8 Dead-End Record, SC-9 Resume From Recorded State, SC-1.3 No Dead Fields, and a carrier map of the six handoff sections (§4.4). New `l2-session-checkpoint.md` 1.0.0: the task-start record, the `Attempts` field, one shared read-only resume script and its call sites, the checkpoint claim in finalize output, retirement of the fill-percentage triggers, removal of a dead field and a phantom command, and the full regression case list (H1–H10, C1–C5). `l2-status-command.md` 1.1.0 → 1.2.0 (in-flight branch calls the shared predicate; "no writes" clarified), `l2-test-suite.md` 1.17.0 → 1.18.0 (coverage mandate), `l2-workflow-wrappers.md` 1.2.0 → 1.3.0 (`PHANTOM_COMMAND`), `l2-engine-finalization.md` 3.2.1 → 3.2.2 (cross-reference, patch). Post-Update Review found defects in the first draft — a C15 scope leak in the resume script, a DG-1 contradiction, an undefined resume-completion moment, undefined terms — all fixed before promotion. Specification only, so no engine change and no C14 bump; the thirteen-row deployment inventory (`l2-session-checkpoint.md` §7) is routed to `/magic.task engine`. The pause snapshot stays a valid optional path; its retirement is deferred pending usage evidence.)
- **Previous**: 2026-09-20c (Spec count 33 → 35: two added, one amended, one patched. `/magic.spec engine` blank trigger — DA-3 selected the plan's one open Backlog item, the only thing gating the `/magic.task` funnel (`DESIGN_DEBT_PENDING`): `l2-finalize-state-accuracy.md` had crossed `check-bloat.js`'s hard `SPEC_DECOMPOSE` tier (552 lines against 500; 476 when the day began). Decomposed 1.8.0 → 2.0.0 along the seam its defects already show: the register stays and keeps what **calls** the state writer (§2, §4, §9, §11), while the `update-state.js` writer's defects moved verbatim to two new children — `l2-update-state-structure.md` 1.0.0 (locating, grouping, creating: §8, §8.5, §12, §12.1, §13, §13.1, §13.2; 262 lines) and `l2-update-state-values.md` 1.0.0 (progress, replacement-string safety, the line cap: §3, §5, §6, §6.1, §7, §10; 222 lines). Defect numbers stay permanent, unlike the parent's own 2.0.0 decomposition, which renumbered: engine code comments, harness comments and other specs cite `§N` of this file, and a specification pass cannot retarget them, so a new Defect Register maps every number to its file (§14/§15 retired). Audited as a multiset of lines against 1.8.0: the only lines that differ are reworded place-bound phrases, retired headings and three stale "Open obligation" Regression Coverage labels (§8, §9, §10 — closed by Phases 19 and 23) restated as the requirements they are. `l2-engine-finalization.md` 3.2.0 → 3.2.1: one stale `§10` reference retargeted to the register's unnumbered Known Gaps section. L1 untouched — `Implements:` edges are declared by the L2s and the register keeps its name — and no spec `Implements:` the register, so no C12 cascade. `check-bloat.js` now reports no `SPEC_DECOMPOSE`; `l2-engine-diagnostics.md` stays at its Parked `SPEC_BLOAT` (388). The Backlog item is closed in `PLAN.md`; `/magic.task engine` registers the two new specs. No engine change, so no C14 bump.)
- **Previous**: 2026-09-20b (No spec-count change, 2 amended. User-directed follow-up to the entry below: the three items it recorded as Known Gaps and deliberately left are closed. `l2-finalize-state-accuracy.md` 1.7.0 → 1.8.0 gains §13.1 (the scalar-field loop and the `## Progress` recompute silently skipped a write whose anchor was absent — a `--next-action`, `--status` or `--handoff` accepted and dropped on a hand-trimmed file — and every field pattern was unanchored, so a label quoted mid-line was rewritten from the label onward) and §13.2 (the line-cap guard used the same substring search, required a following heading, and removed the oldest entry by looking its text up again — a last-position `## Recent Decisions` was never pruned yet reported exhausted, and a CRLF file reported a prune that did not happen); sixteenth and seventeenth defects, all implemented, reproduced directly against 2.1.101 before either fix. Fix: line-anchored field patterns, `ensureField()` creating an absent field at its template position (and its container section when that is gone too; `Updated` deliberately not created), an absent `## Progress` created holding the counters and a fence-less heading left untouched and announced, and the guard locating by heading and removing by position through `entryRanges()`, now the single grouping primitive `collectEntries()` derives from (differentially fuzzed: 80 000 comparisons, no difference). The three Known Gaps leave §15. `l2-engine-diagnostics.md` 1.2.1 → 1.2.2: inventory rows for `STATE_SECTION_CREATED`, `STATE_FIELD_CREATED` and `PROGRESS_BLOCK_UNRECOGNISED` (the table stays a checklist, not the contract). `dev/tests/engine.js` 114 → 124, mutation-checked (12 mutations, each caught). C14 bump 2.1.101 → 2.1.102. No spec status transition.)
- **Previous**: 2026-09-20 (No spec-count change, 1 amended. MAGIC-SPEC ENGINE BUG REPORT (engine 2.1.100) direct repair: `update-state --decision` printed `STATE.md updated` but wrote nothing when the file had no `## Recent Decisions` heading. Reproduced directly — with no version lag, the report's file/line reference matched HEAD — and found to be one shape at two adjacent call sites: `addConstraint` dropped its write identically, both guarded by `if (secStart !== -1)` with no alternative branch. A second manifestation of the same root surfaced during reproduction: the `indexOf` substring search also matches a line that merely quotes the heading, so with the heading absent the rebuild was spliced into that line, destroying its tail. The reporter's committed history showed how a heading goes missing (whole-block hand edits no writer can produce), so the engine now creates the section: line-anchored `locateSection()`, create-if-absent `ensureSection()` in template order, a stderr notice and a `fix`-severity `STATE_SECTION_CREATED` diagnostic. `l2-finalize-state-accuracy.md` 1.6.1 → 1.7.0 gains §13 (fifteenth defect, all implemented; Regression Coverage and Known Gaps renumbered §14/§15) plus two Known Gaps found and deliberately not fixed here — every other write whose anchor is absent is still skipped silently, and the line-cap prune never runs when `## Recent Decisions` is the last section. `dev/tests/engine.js` 107 → 114, mutation-checked. C14 bump 2.1.100 → 2.1.101. No spec status transition.)
- **Previous**: 2026-09-19f (No spec-count change, 1 amended. Source-hygiene fix found while reading the collector: `lib/diagnostics.js`'s dedup key joined `severity`, `source` and `code` with two literal NUL bytes instead of the `\0` escape — the only NUL in the 88 files of the engine kernel. Runtime-identical, but git classified the file `-text` (the `eol=lf` normalisation never applied to it) and GNU grep answered `Binary file matches` instead of the matching lines. `l2-engine-diagnostics.md` 1.2.0 → 1.2.1: §4.6 names the U+0000 separator and forbids the raw byte; Regression Coverage §6.16 (dedup separator) and §6.17 (no raw NUL anywhere in the kernel). A 600-set differential confirmed identical behaviour. `dev/tests/engine.js` 105 → 107, mutation-checked. C14 bump 2.1.99 → 2.1.100. No spec status transition.)
- **Previous**: 2026-09-19e (No spec-count change, 1 amended. The open item the previous entry recorded is done: an `ENGINE_INTEGRITY` warning said only `modified locally` — alike for an edit, a line-ending conversion and a swapped manifest — and recorded no hash, so the field report's flagged-then-identical pair of files could not be explained. `l2-engine-automation.md` 1.15.1 → 1.16.0 gains the **Diagnosable drift** paragraph: new `utils.describeManifestDelta()` (both hashes, the file's own line-ending style, whether line endings are the whole difference — either direction, lossless, total); `check-prerequisites.js` messages carry both 12-hex hash prefixes and name a line-endings-only difference, `update-engine-meta`'s `Detected change in:` line names it too. No change to verdicts, exit codes or remedies. `dev/tests/engine.js` 102 → 105, mutation-checked; `docs/checksums.md` §3.1 updated. C14 bump 2.1.98 → 2.1.99. No spec status transition.)
- **Previous**: 2026-09-19d (No spec-count change, 1 amended, documentation only. The reporting project's session log was located and read. It confirms the user-installation defect on real data — the hinted `update-engine-meta` moved that project's engine version 2.1.95 → 2.1.96 with a success line and an unchanged warning — and narrows, without explaining, the original `spec.md`/`task.md` mismatch: clean pre-flight, then exactly those two files about 40 minutes later; the copies read in between byte-identical to the release; no write under `.magic/` in the log; identical to the release again afterwards. `l2-engine-automation.md` 1.15.0 → 1.15.1 corrects one mechanism-only claim ("produced phantom upgrade drift" → "indistinguishable from an upgrade") and records the diagnosability gap as open. No engine change, no C14. No spec status transition.)
- **Previous**: 2026-09-19c (No spec-count change, 1 amended. A downstream agent's side note — `ENGINE_INTEGRITY` fired for `.magic/spec.md` and `.magic/task.md` — investigated. The cause of that mismatch was not established: the latest release archive and every manifest since v2.1.87 match their own blobs, and the inspected consumer installs all pass their manifests with those files byte-identical to upstream. Reproducing the scenario in a user-installation fixture did expose a confirmed defect in the remedy path: `update-engine-meta`, named as the fix by `check-prerequisites.js` and by its own `--check` failure line, bumped `.magic/.version` on every run in a user installation, could not regenerate the manifest, printed a success line and exited 0 — the warning persisted while the version ratcheted, which Engine Upgrade Detection reads as a phantom external upgrade. `l2-engine-automation.md` 1.14.0 → 1.15.0 gains the **User-installation boundary** paragraph (§Engine Meta Update Flow): write mode is refused without touching anything (exit 1), and `--check` and the `ENGINE_INTEGRITY` warning name a restore from the release archive there while keeping C14 in the developer repository. `dev/tests/engine.js` 99 → 102, mutation-checked. C14 bump 2.1.97 → 2.1.98. No spec status transition.)
- **Previous**: 2026-09-19b (No spec-count change, 1 amended. User-directed follow-up to the entry below: the lazy-continuation gap `l2-finalize-state-accuracy.md` §12.1 recorded as accepted is closed for list-item fields. 1.6.0 → 1.6.1 — the Required Fix's continuation rule widened from indented-only to indented-plus-lazy for `- **Task:**` / `- **Spec:**` / `- **Next Action:**` (CommonMark lazy continuation, behind a closed stop-set that errs toward stopping; header fields stay indented-only). Replayed every committed `STATE.md` version of seven real projects (1025 versions) through the old and new rules — byte-identical output, no structure consumed — so safe on real data, though no file in that corpus wraps lazily (prophylactic, not observed). `dev/tests/engine.js` 97 → 99; each stop-set member mutation-checked. C14 bump 2.1.96 → 2.1.97. No spec status transition.)
- **Previous**: 2026-09-19 (No spec-count change, 1 amended, registry sync included. MAGIC-SPEC ENGINE BUG REPORT (engine 2.1.95) direct repair: `update-state` replaced only the first line of an existing multi-line `**Task:**` / `**Next Action:**` bullet, leaving its old continuation lines behind as orphaned prose under the new text. The report blamed the line-cap guard's auto-pruning; investigation cleared it (§12's prune fix holds, and the orphans appear at any file length) in favour of the scalar-field patch loop — §12's own fix reopened in a call site it never audited. Reproduced directly and against the reporter's own committed `STATE.md` history (byte-identical `## Current Position`). `l2-finalize-state-accuracy.md` 1.5.0 → 1.6.0 gains §12.1 (fourteenth defect, all implemented) plus one new Known Gap found and deliberately not fixed here — the line-cap prune never matches in a CRLF file yet reports success; this row also reconciles the 1.4.0 → 1.5.0 VERSION_DRIFT the previous amendment left. `dev/tests/engine.js` 94 → 97. C14 bump 2.1.95 → 2.1.96. No spec status transition.)
- **Previous**: 2026-09-17d (No spec-count change, 2 amended via `/magic.spec` dispatch of a MAGIC-SPEC ENGINE BUG REPORT (engine 2.1.93). `l1-engine-diagnostics.md` 1.0.1 → 1.1.0: new **DG-10** (Revalidation Before Render) + §1.6 Motivation — `check-prerequisites` records condition findings (`SYNC_GAP`, `ORPHANED_SPEC`, etc.) at a workflow's own Pre-flight; by the time finalize drains the sink, that same invocation's own writes (e.g. `magic.task` regenerating `PLAN.md`/`TASKS.md`) may have already resolved the condition, and the digest rendered it as still open — confirmed by an immediate manual `check-prerequisites --json` re-run returning `warnings: []`. A finding MAY now declare a read-only `recheck` reference; revalidation reruns it once per signature immediately before every render (preview included) and drops a finding whose code no longer reproduces — filter only, never a new-finding source, failure-open per DG-9. Post-Update Review found one defect before promotion: an unguarded recheck spawns the emitter's own check, which records through DG-1 like any other invocation, silently re-queuing a still-open condition into the very sink revalidation is reading — closed by a new self-reference guard (`MAGIC_DIAGNOSTICS_SUPPRESS`) suppressing recording for the duration of a recheck. `l2-engine-diagnostics.md` 1.1.0 → 1.2.0: implements DG-10 — `revalidate()` added to the collector contract and wired into `finalize.js` between `drain()`/`read()` and `emitTail()`; `check-prerequisites.js`'s `warn()` now attaches `recheck` to every finding (its only emitter, since every one of its warnings is a condition, never an event); 7 new regression coverage cases. Both specs: Post-Update Review (5-lens) + Instruction Quality Pass PASS after the fix; Trust Mode (C9) auto-promoted `Stable → RFC → Stable` within this pass. Engine-side code (`finalize.js`/`diagnostics.js`/`check-prerequisites.js`) is out of `/magic.spec`'s write scope — routed to `/magic.task engine`.)
- **Previous**: 2026-09-17c (No spec-count change. `/magic.run engine` Phase 29 Execute found `l1-engine-core.md` 1.7.0's Concept-Only Classification Required Fix mis-targeted `l2-engine-automation.md`'s `analyze-coverage.js`-backed `EXEMPT` classification by a vocabulary analogy ("coverage" ↔ "coverage") rather than by reading the actual mechanism — the real "Bare L1 without L2 children" advisory is a cognitive check in `.magic/analyze.md` §Advisory Report Categories with no script behind it at all. Corrected before any code was written: `l1-engine-core.md` 1.7.0 → 1.7.1 (Required Fix re-targeted, simplified — no persisted marker-clearing step needed since the advisory only fires when no L2 child exists); `l2-engine-automation.md` 1.11.0 → 1.12.0 (the mis-targeted addition fully reverted). The real fix landed as one instruction sentence in `.magic/analyze.md` (engine 2.1.85 → 2.1.86, tagged `magic.analyze`) — Phase 29 complete. Also folded in this pass: an ad hoc, user-directed fix earlier the same session, unrelated to Phase 29 — `generate-context.js` appended a redundant trailing newline (double blank line at EOF, `wc -l` 101 → 100 after the fix); engine 2.1.84 → 2.1.85.)
- **Last Updated**: 2026-09-13b (No spec-count change, registry-only. `/magic.task engine`'s Pre-flight (`--verify-headers`) caught three pre-existing VERSION_DRIFT rows — file headers ahead of the registry on `l2-engine-finalization.md` (3.0.0 → 3.1.0, §6.1 Workbook Name Recognition), `l2-finalize-state-accuracy.md` (1.2.0 → 1.4.0, §8.5 sibling-call-site fix + §11 workspace-scoping defect), and `l1-session-continuity.md` (2.0.0 → 2.1.0, Zero-Row Vacancy's vacant-section case) — none caused by this session; each file's own Document History already carries a passed Post-Update Review for the bump, so this was a registry-sync gap, not unreviewed content, per RE-3's resolution path. Reconciled directly (no spec content touched) so `/magic.task engine` could proceed. Fourth such gap found this session (after `l2-engine-automation.md` during the preceding `/magic.spec` pass) — worth a future Known Process Gaps entry on `update-engine-meta`'s registry-sync reliability if a fifth instance surfaces.)
- **Previous**: 2026-09-13 (No spec-count change, 3 amended. Reference-mining pass against a downstream consumer project's own SDD constitution surfaced two findings the engine's own corpus did not yet cover. `l1-engine-core.md` 1.6.0 → 1.7.0: new **Concept-Only Classification** entry under Known Process Gaps — a non-blocking `PLAN.md`-facing `concept-only` marker as an alternate resolution to the Concept/Implementation Debt Asymmetry, distinct from the 2026-08-07 rejected debt-ceiling (that blocked authoring; this reclassifies what the coverage-gap advisory counts, addressing a signal-quality gap neither the rejected ceiling nor SC-2.4's `DESIGN_DEBT_PENDING` gate reaches). `l2-engine-automation.md` 1.9.0 → 1.11.0: two changes — (a) reconciled a pre-existing VERSION_DRIFT (registry had not been synced past 1.9.0 following the file's own 1.10.0 Zero-Row Vacancy amendment, caught by RE-3 before this pass's own edit); (b) 1.10.0 → 1.11.0 implements the `concept-only` mechanism (§Coverage Denominator Scope extension), whether the `PLAN.md`-tagging behavior needs a new C-numbered convention left to `/magic.rule`. `l1-sdd-reference-containment.md` 1.4.0 → 1.5.0: new §2 assumption — the scaffold/product boundary is provisional by design (a consumer project may eventually self-host its own development discipline), complementing RC-12's removal test with the anticipatory direction; no new Core Invariant. All three: Post-Update Review (5-lens) and Instruction Quality Pass found no blocking issues; Trust Mode (C9) auto-promoted each back to `Stable` within the same invocation. A third finding (usage-simulation-companion convention) was judged documentation-only and not actioned as an engine change.)
- **Previous**: 2026-08-28 (+1 spec — 1 new L1. Owner directive: `/magic.spec {idea}` must refine an under-determined idea through clarification *before* generating specifications, while the agent solves technical questions itself and phrases any question for a non-specialist. Created `l1-idea-intake-gate.md` 1.0.0 — the input-side twin of `l1-prompt-quality-gate.md`: the engine reviews every prompt it writes and none of the prompts it receives, and the idea is the first prompt in the chain. IK-1..IK-9 register the gate as C27 Escalation Whitelist entry E6 without loosening DA-1/DA-3/DA-9 — Selection, Sequencing, and proposal surfaces stay declarative, and IK-3 permanently bars technical-realization questions from the user channel. Three owner decisions recorded as rejected alternatives: unbounded rounds over a three-question cap (§6.2), blocking over non-blocking (§6.3), chat-only over a persisted clarification record (§6.4). Termination is guaranteed by IK-6's strict-progress test rather than a round counter — a non-convergent reply, including "you decide", ends the gate immediately and falls through to Draft + TBD. Deployment §5 routes seven engine touch-points (incl. the DA-2 table, `.design/RULES.md` + `.magic/templates/rules.md` C27 parity, and `rules/magic.md` §7) to `/magic.task engine`; all are outside `/magic.spec`'s write scope. Post-Update Review (5-lens) and Instruction Quality Pass passed; promoted `Draft → Stable` via Trust Mode (C9).)
- **Previous**: 2026-08-27 (No spec-count change. MAGIC-SPEC ENGINE BUG REPORT (field agent, downstream project) direct repair: `update-state.js`'s `fieldMap` loop patched scalar STATE.md lines with a string-form `.replace()`, so a `$'` / `` $` `` / `$&` in `nextAction` or `task` — task titles carry bash ANSI-C quoting routinely — expanded to the match's surrounding context, truncating `Next Action` and duplicating every section below it including a stale `## Progress` counter. Same class as §6, which its own sweep wrongly cleared (it checked only `$`-digit). Fixed to the function-form `.replace()` §6 already uses; `l2-finalize-state-accuracy.md` 1.1.2 → 1.2.0 gains §6.1 retrospec (§8 precedent) + one §11 regression bullet; `dev/tests/engine.js` 68 → 69, full suite green. C14 bump. No spec status transition.)
- **Previous**: 2026-08-27 (No spec-count change. User directive: retire the commit-message suggestion feature entirely — the engine must no longer compose or print a suggested commit message on any finalize path; committing is left entirely to the user. Retired SC-3/SC-3.1 in `l1-session-continuity.md` (1.11.0 → 2.0.0) and propagated the removal through every implementation-surface spec: `l2-engine-finalization.md` (2.0.0 → 3.0.0, §5.2 + §8 + `--no-commit-msg` flag), `l2-finalize-output-contract.md` (1.2.0 → 2.0.0, §3 rescoped to stdout-listing completeness alone), `l2-finalize-state-accuracy.md` (1.1.1 → 1.1.2, cross-reference only), `l2-status-command.md` (1.0.0 → 1.1.0, dropped SC-3 compliance row), `l2-test-suite.md` (1.14.0 → 1.15.0, dropped the SC-3 coverage case), `l1-engine-diagnostics.md` (1.0.0 → 1.0.1, cross-reference only), `l2-engine-diagnostics.md` (1.0.0 → 1.1.0, `emitTail()` drops the commit notice). The pre-existing hard rule — the engine and agent never invoke a write-side git operation — is unaffected. Post-Update Review (5-lens) and Instruction Quality Pass found no blocking issues across all eight specs; each reverted `Stable → RFC` under the Amendment Rule and was auto-promoted back to `Stable` via Trust Mode (C9) within the same invocation.)
- **Previous**: 2026-08-22 (No spec-count change. Dispatched a MAGIC-SPEC ENGINE BUG REPORT (engine 2.1.73, consumer workspace `engine`) via `/magic.spec`: `l2-finalize-output-contract.md` 1.1.0 → 1.2.0 gained §4.5 — the report reproduced the already-documented §4.1 vocabulary-exhaustion defect via the `run` case; its proposed fix (interpolate a phase/task identifier into the bullet) was rejected as a direct RC-11 violation already barred by §4.4 constraint 1. The real gap: §4.4's `release-changelog` remedy already exists (confirmed reachable via `executor.js`'s generic dispatch, no wiring needed) but finalize's `CHANGELOG` stdout row never names it when deduped — verified against this repository's own unrotated `[Unreleased]` section. Required Fix routed to `/magic.task engine` (out of `/magic.spec`'s write scope). Post-Update Review (5-lens) and Instruction Quality Pass found no blocking issues; retained `Stable` via Trust Mode (C9) amendment cycle.)
- **Previous**: 2026-08-22 (No spec-count change. User-directed revisit of a Parked backlog item, not a field report: l1-engine-core.md 1.5.0 → 1.6.0 gained a resolved Known Process Gaps entry, **Dev-Repo Engine-Version Snapshot Sync** — resolved as a dev-repo-only exemption reusing `update-engine-meta.js`'s existing `dev/scripts/generate-checksums.js` presence guard. Required Fix implemented the same session in Phase 24 — engine 2.1.73 → 2.1.74, harness 66 → 68.)

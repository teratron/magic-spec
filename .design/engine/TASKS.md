# Master Task Index (Registry)

**Version:** 1.32.0
**Generated:** 2026-08-28
**Based on PLAN:** .design/engine/PLAN.md v1.33.0
**Based on RULES:** .design/RULES.md v1.10.0
**Execution Mode:** Parallel
**Status:** Active

## Overview

Tactical registry of all phases and their statuses. **Plan complete — no active phase.** Phase 28 (last completed) closed the two engine defects Phase 27 hit in production — retrospective R25 and R26, which are the same defect twice: tooling that reports success while shipping stale output. `workflows/*.md` are per-file hardlinks to `.agents/workflows/*.md`, the tree `sync-skills` actually reads, but `validate-hardlinks.js` hardcoded two groups (AGENTS family, `rules/`) and the `[C-001]` constraint text named one directory — so a wrapper edit delinked the pair invisibly and C14 published a skill wrapper built from pre-edit content while every integrity check reported success. The repair was then blocked by the second defect: change detection reads `.magic/` checksums only, so a `workflows/`-only change reported "No changes detected in engine core" and skipped regeneration entirely. Planning narrowed the retrospective's own recommendation after reading the source: widening the checksum manifest was **barred**, since `update-engine-meta.js` documents that exclusion as a deliberate contract protecting user-customizable wrappers and partial installations, so the fix bound the regeneration *trigger* instead, leaving the read-only `--check` path (hardcoded in the user's pre-commit hook) untouched. Planning also established that `skills/` is **not** a linked pair — `sync-skills` generates independently into both trees — recorded in the spec so no one later "fixes" a fourth pair that must not exist. All 7 tasks closed with mandatory negative controls on every new harness case, each confirmed to fail against reverted/pre-fix code before being confirmed green — including the actual pre-fix `validate-hardlinks.js` sourced live from `git show HEAD`, not a reconstruction. Harness 69 → 72; engine 2.1.79 → 2.1.80. Graduated directly from the retrospective, same pattern as Phases 20/21/25/26 — the first time that graduation pattern applied to retrospective-sourced items rather than spec-authored ones. 33 specs registered and Stable (INDEX v1.24.0 at plan time). Phase 27 (previously completed) deployed the Idea Intake Gate ([l1-idea-intake-gate.md](specifications/l1-idea-intake-gate.md) v1.0.0, IK-1..IK-9), authored by the `/magic.spec` pass immediately preceding this planning invocation on an explicit owner directive: `/magic.spec {idea}` must refine an under-determined idea through clarification before generating specifications, while the agent resolves technical questions itself and phrases any question for a non-specialist. The engine routes every artifact it authors through the `prompt-engineer` quality gate and reviews nothing it receives, yet the idea is the first prompt in the chain — and an essence-level defect there is not deferred by a `TBD` marker but committed by the surrounding prose and inherited by plan, tasks, and code. The phase is reconciliation rather than addition: `.magic/spec.md` asserts the opposite rule in three places, and planning surfaced the third (the Mode Transition `Auto-Transfer` clause's one-round cap, contradicting IK-6's uncapped convergent dialogue) which the spec's own §5 deployment table had missed. Whitelist entry E6 must register atomically across four surfaces — the DA-2 table, `.design/RULES.md`, `.magic/templates/rules.md`, and `rules/magic.md` — since a constitution disagreeing with the engine that enforces it is a release blocker. Track A gates D/F/T; B, C, E are file-independent; `skills/magic-spec/SKILL.md` is generated by C14's `sync-skills`, so the wrapper edit must precede the bump. **[C-001]** applies to Track C. 33 specs registered and Stable (INDEX v1.24.0 at plan time). Phase 26 (last completed) implemented the code side of the commit-suggestion retirement — the `/magic.spec` pass immediately preceding that planning invocation already retired SC-3/SC-3.1 in [l1-session-continuity.md](specifications/l1-session-continuity.md) v2.0.0 and propagated it through [l2-engine-finalization.md](specifications/l2-engine-finalization.md) v3.0.0 and [l2-finalize-output-contract.md](specifications/l2-finalize-output-contract.md) v2.0.0; by explicit user directive the engine must no longer compose or print a suggested commit message on any finalize path, and this phase strips the composing functions from `commit-suggester.js`, the flag/config/rendering machinery from `finalize.js`, and the dead `suggestCommit` key from both `workspace.json` files — no design work remains, the item graduated straight from Backlog (same pattern as Phases 20/21/25). 32 specs registered and Stable (INDEX v1.23.3 at plan time). Phase 25 (last completed) implemented the §4.5 CHANGELOG dedup discoverability fix ([l2-finalize-output-contract.md](specifications/l2-finalize-output-contract.md) v1.2.0): a field report against engine 2.1.73 reproduced the already-documented §4.1 vocabulary-exhaustion defect via the `run` case, proposing a fix that would have violated RC-11; the real gap was that `finalize.js`'s deduped `CHANGELOG` stdout row never named the `release-changelog` remedy that already exists — live-verified, this very phase's own finalize run printed the hint for the first time. One-line diagnostic-surface change plus regression coverage for a CHANGELOG-write path this harness had never exercised through a live CLI run before. Harness 68 → 69; engine 2.1.74 → 2.1.75. Phase 24 closed the dev-repo Engine-Version snapshot exemption ([l1-engine-core.md](specifications/l1-engine-core.md) v1.6.0): a C14 bump in this engine's own repo now also refreshes the `**Engine Version:**` snapshot in `.design/INDEX.md`, so Engine Upgrade Detection stops narrating drift for first-party changes it was never designed to catch — live-verified, the snapshot jumped `2.1.72` → `2.1.74` directly on this phase's own C14 bump. Consumer installations are unaffected by construction, isolated-fixture-confirmed. The phase also carried Track C, a scope extension its own planning run surfaced: `synthesizeNextAction()` was taking the display title from `stripQuoted()` content, blanking any inline code span in a task title out of the persisted `Next Action` — a live regression from Phase 23's SH-1 binding, fixed by a line-indexed recovery from the raw source. Harness 66 → 68; engine 2.1.73 → 2.1.74. 32 specs registered and Stable (INDEX v1.23.2 at plan time). Phase 23 closed the two `Required Fix` blocks authored against engine 2.1.72 by the `/magic.spec` pass that preceded planning: `synthesizeNextAction()`'s tier-2 loop now screens the matched task's own `## Detailed Tracking` `Status`/`Assignment` fields, not only phase-level `Blocked` signals — a healthy phase could still surface a blocked, user-assigned task as `/magic.run`-executable ahead of an actionable one (l2-finalize-state-accuracy.md v1.1.1 §9 / l1-session-continuity.md v1.11.0 SC-2.1(c)); and the decision-prune preamble no longer claims an archival to `PLAN.md` that no code path performs (§10, in both `update-state.js` and its bootstrap template). Two implementation bugs were caught and fixed before either task was marked Done (a `$`-under-`m`-flag regex lookahead collapsing to empty; a plural-agreement typo in the realigned test assertion) — both replayed end-to-end against the real `finalize.js` CLI, not only unit fixtures. Harness 65 → 66; engine 2.1.72 → 2.1.73. 32 specs registered and Stable (INDEX v1.23.2). Phase 22 closed field-report triage: `DESIGN_DEBT_PENDING`'s plan-complete predicate is no longer structurally unreachable once a phase has been archived under the canonical single-table `tasks.md` template (l2-engine-automation.md v1.9.0 / l1-session-continuity.md v1.10.0 Terminal-Row Recognition, distinct from Phase 21's counter fix — live-verified against this workspace's own post-archival `TASKS.md`, whose single `## Active Phases` table now correctly reads as plan-complete); `analyze.md` Mode C Depth Control bypass ambiguity resolved to never-HALT (l1-engine-core.md v1.5.0); Project-auditor role card's stale "Invariant 6" citation corrected to `.design/RULES.md` C13 §5 (l2-role-cards-governance.md v1.1.1). Harness 64 → 65. 32 specs registered and Stable (INDEX v1.23.0); engine at 2.1.72. Phase 21 implemented `check-prerequisites.js` registry-scan hygiene (l1-scan-input-hygiene.md v1.2.0) + `DESIGN_DEBT_PENDING` Parked-marker counter support (l1-session-continuity.md v1.9.0 SC-2.4 addendum) — both closed by design in that session's `/magic.spec` pass, planned directly as Engine Improvement (explicit user authorization) after `/magic.task`'s Pre-flight HALTed on the same `DESIGN_DEBT_PENDING` gate a second time with no remaining design work to absorb it. Phase 20 implemented the two backlog items closed by the `/magic.spec` pass that followed Phase 19's own first-ever `DESIGN_DEBT_PENDING` firing: `release-changelog.js` (R11 remainder — explicit opt-in CHANGELOG rotation, since magic-spec cannot observe a downstream consumer's release event) and the `EXEMPT` coverage classification (`.design/` bookkeeping/journal files no longer count against `analyze-coverage.js`'s reported percentage — 85.4% → 96.5% on this workspace). Harness 57 → 62. Phase 19 implemented the four contracts authored by the 2026-08-07 spec pass that also decomposed `l2-engine-finalization.md` at 367 lines: R10 closed (archival index rewrite — confirmed live on the phase's own PLAN.md entry, ending a run of five consecutive hand-corrections); R13 closed (`DESIGN_DEBT_PENDING` Pre-flight signal, `l1-session-continuity.md` SC-2.4 — the SC-2.2 ↔ SC-2.4 tension resolved by routing the signal through Pre-flight's HALT rather than touching `computeNextAction()`, exactly as recorded in PLAN.md before implementation); R12 closed (structural harness coverage for the `addDecision` fix that had shipped ahead of its spec, negative-controlled against the reconstructed pre-fix logic); R11 advanced but not closed (the duplicate-`### Changed`-heading anomaly root-caused via `git blame`/`git show` to two 2026-05 manual edits predating the automated writer — no CHANGELOG fix was written, per the investigation's own governing constraint; routes to `/magic.spec` for the follow-up). Harness 55 → 57. Phase 18 implemented the Engine Diagnostics Digest (`l1-engine-diagnostics.md` DG-1..DG-9, `l2-engine-diagnostics.md`): non-fatal engine findings are now recorded to a sink and delivered as one digest immediately before the next step, closing a defect found during authoring — the Finalization Protocol binds the agent to relay **stdout**, while all 17 non-fatal findings across 6 scripts were written only to **stderr**, invisible by contract rather than by any one script's oversight. Twelve tasks across five tracks (A gated all; B preceded D on shared `finalize.js`; E — the sole track outside C14 tracking, carrying **[C-001]** — was file-independent of the rest); harness 46 → 55. INDEX v1.15.15 carried nine spec amendments authored since PLAN v1.15.2 and never implemented — Phases 15-16 were that implementation backlog, not new scope, and it is now closed. Phase 15 implemented the eight `Required Fix` blocks in l2-engine-finalization 1.10.0 §7-§10 (SC-1.2, SC-2.1(a), SC-2.3, SC-3.1, RC-11), all re-verified present in engine 2.1.62 before execution; harness 34 → 43. Phase 16 deployed the two prose-only amendments (WI-10 documentation parity across all three `init.md` claim surfaces plus the cognitive suite, RC-12 scaffold-removal check in ventilation and ambient rules). R8 documented (l2-release-pipeline §5.3); R9 (AGENTS hardlinks) operational — `/magic.dev.init`; R10, R11 (new this phase — CHANGELOG dedup suppresses genuine entries), the §8.7 known gap, and the pending debt-ceiling convention are tracked in PLAN.md Backlog with their routing.

***(Overview de-triplicated 2026-08-22:*** *the Phase 20/19/18 narrative had accumulated verbatim three times through earlier update cycles — an append-not-replace artifact, recorded as a diagnostic during Phase 23's finalize and collapsed to a single copy here, per that finding's own stated remedy. No content was lost; two exact duplicates were removed.)*

## Active Phases

*None — plan complete. Run `/magic.task engine` to plan new scope.*

## Completed Phases

| Phase | Description | Status |
| --- | --- | --- |
| [Phase 28](archives/tasks/phase-28.md) | Silent-Failure Pair: Link Coverage & Regeneration Trigger — linked-pair inventory + table-driven validator covering `workflows/`, widened `[C-001]`, skill regeneration decoupled from the checksum verdict, harness negative controls | `Done (Archived)` |
| [Phase 27](archives/tasks/phase-27.md) | Idea Intake Gate Deployment (E6) — input-side clarification gate installed in `.magic/spec.md`, E6 registered across the DA-2 table + live constitution + distributed template + ambient rules, reviewer checks on the `prompt-engineer` card, user docs, cognitive suite coverage | `Done (Archived)` |
| [Phase 26](archives/tasks/phase-26.md) | Commit-Suggestion Feature Removal — strip message-composition from `commit-suggester.js`, flag/config/rendering machinery from `finalize.js`, dead config key from both `workspace.json` files | `Done (Archived)` |
| [Phase 25](archives/tasks/phase-25.md) | CHANGELOG Dedup Discoverability Hint — `finalize.js`'s deduped row names `release-changelog`; field-confirmed §4.5 fix, no bullet-content or dedup-logic change | `Done (Archived)` |
| [Phase 2](archives/tasks/phase-2.md) | Skill Projection & Agent Surface Implementation | `Done (Archived)` |
| [Phase 3](archives/tasks/phase-3.md) | Unified Role System — `.magic/roles/` + workflow integration | `Done (Archived)` |
| [Phase 4](archives/tasks/phase-4.md) | Prompt Quality Gate — `prompt-engineer` card #14 + five workflow gates | `Done (Archived)` |
| [Phase 5](archives/tasks/phase-5.md) | Decision Autonomy — C27 constitution anchoring + DA-6 posture + role binding | `Done (Archived)` |
| [Phase 6](archives/tasks/phase-6.md) | SDD Reference Containment — ambient rules + RC-5/RC-6 card gates + RC-7 leak scan | `Done (Archived)` |
| [Phase 7](archives/tasks/phase-7.md) | Shipped Self-Containment — RC-9 purge of 15 engine-workspace references | `Done (Archived)` |
| [Phase 8](archives/tasks/phase-8.md) | Session Continuity & Status Command — finalize SC-2/SC-3 wiring + `/magic.status` surface | `Done (Archived)` |
| [Phase 9](archives/tasks/phase-9.md) | DA-9 Engine Deployment — proposal surfaces (spec.md / analyze.md / task.md) to narrate-and-act form | `Done (Archived)` |
| [Phase 10](archives/tasks/phase-10.md) | Session-Continuity Hardening — plan-state-aware next-action + finalize test coverage | `Done (Archived)` |
| [Phase 11](archives/tasks/phase-11.md) | Archiver Eligibility Fix (R7) — anchored checklist match + re-archive phase-10 | `Done (Archived)` |
| [Phase 12](archives/tasks/phase-12.md) | Wrapper-Body Parity Check (R4) — WRAPPER_BODY_DRIFT in analyze Mode C | `Done (Archived)` |
| [Phase 13](archives/tasks/phase-13.md) | Upgrade-Detection DA Alignment — §1 `[y/n]` → single-path narration | `Done (Archived)` |
| [Phase 14](archives/tasks/phase-14.md) | Shipped Reference Hygiene & Documentation Sync — skill-projection extension guard, `rules/magic.md` case purge, docs sync, SDD-layer backfill | `Done (Archived)` |
| [Phase 15](archives/tasks/phase-15.md) | Finalize-Pipeline Accuracy & Generator Containment — §7-§10 fixes + 9 harness cases | `Done (Archived)` |
| [Phase 16](archives/tasks/phase-16.md) | Documentation Parity & Scaffold Boundary — WI-10 init surfaces + RC-12 §4.4 check | `Done (Archived)` |
| [Phase 17](archives/tasks/phase-17.md) | Scan Input Hygiene — shared strip helper + SH-1/SH-3/SH-4 bindings across four scans | `Done (Archived)` |
| [Phase 18](archives/tasks/phase-18.md) | Engine Diagnostics Digest — collector + JSONL sink, finalize tail emitter (digest → next step), agent channel, 17-emitter migration | `Done (Archived)` |
| [Phase 19](archives/tasks/phase-19.md) | Finalization Contract Fixes — archival index rewrite, plan-complete Pre-flight signal, decision-section coverage, CHANGELOG anomaly root-cause | `Done (Archived)` |
| [Phase 20](archives/tasks/phase-20.md) | Backlog Implementation — Release Rotation (R11 remainder) & Coverage Denominator Scope (EXEMPT) | `Done (Archived)` |
| [Phase 21](archives/tasks/phase-21.md) | check-prerequisites.js Registry-Scan Hygiene & Backlog Counter | `Done (Archived)` |
| [Phase 22](archives/tasks/phase-22.md) | Field-Report Triage — DESIGN_DEBT_PENDING Structural Predicate, Mode C Depth Control, Project-Auditor Citation | `Done (Archived)` |
| [Phase 23](archives/tasks/phase-23.md) | Next-Action Task-Level Precedence & Decision-Prune Honesty — Detailed Tracking `Status`/`Assignment` screen, terminal all-excluded branch, decision-prune claim correction | `Done (Archived)` |
| [Phase 24](archives/tasks/phase-24.md) | Dev-Repo Engine-Version Snapshot Sync — L2 snapshot writer, guarded L1 delegation, `rules/magic.md` §1 sole-writer carve-out; plus Track C, the Next Action title-stripping regression found by this phase's own planning run | `Done (Archived)` |

## Backlog

*None at phase level — Phase 28 came from this retrospective's own R25/R26 recommendations, not the Backlog: both remedies were concrete enough at recording time that planning wrote their governing spec sections directly, with no `/magic.spec` pass and no item ever queued. Pre-flight returned `ok: true` with no warnings at plan time — no `DESIGN_DEBT_PENDING`. Phase 27 came through the normal design-then-plan handoff, not the Backlog: the `/magic.spec` pass that preceded it authored [l1-idea-intake-gate.md](specifications/l1-idea-intake-gate.md) from a live owner directive and closed every fork inside it, so no item ever queued. Pre-flight returned `ok: true` with only the two expected warnings (`ORPHANED_SPEC` for the new spec, `SYNC_GAP` on the registry version) — no `DESIGN_DEBT_PENDING`. Phase 26 absorbed the commit-suggestion feature-removal item, which left the Backlog when a second `DESIGN_DEBT_PENDING` HALT on an already-answered design question confirmed no design work remained (same pattern as Phases 20/21/25). Phase 24 absorbs the dev-repo Engine-Version snapshot exemption, which left the Backlog when its design question was answered at explicit user request (the item had sat Parked since 2026-06-12). What remains in [PLAN.md](PLAN.md) Backlog is entirely Parked (the `Status`-recompute question, the `SPEC_BLOAT` watch — now naming `l2-finalize-state-accuracy.md` at 313 lines) or genuinely deferred (`frontend-specialist` role card), none of it forcing ordering against Phase 24. Historical note — the `check-prerequisites.js` registry-scan hygiene fix and the `DESIGN_DEBT_PENDING` counter's Parked-marker support moved from [PLAN.md](PLAN.md) Backlog into Phase 21's scope once a second same-session `DESIGN_DEBT_PENDING` HALT confirmed no design work remained, and the user authorized Engine Improvement directly. The debt-ceiling convention closed by explicit user rejection (Escalation Whitelist E4) this same session — Phases 15-20 already closed the backlog it was meant to gate, with no hard ceiling ever existing. What remains in [PLAN.md](PLAN.md) Backlog is now Parked (the `Status`-recompute question, the dev-repo snapshot drift, the `SPEC_BLOAT` watch) or genuinely deferred (`frontend-specialist` role card) — none of it forces ordering against Phase 21.*

## Meta Information

- **Last Updated**: 2026-08-28
- **Maintainer**: Core Team

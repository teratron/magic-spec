# Example environment variables
# Copy this file to .env (for local/dev) or .env.production (for release)
# NEVER commit .env or .env.production to git

# npm registry authentication token
# Generate at: https://www.npmjs.com/settings/<username>/tokens
# Token type: Automation
NPM_TOKEN=

# PyPI registry authentication token
# Generate at: https://pypi.org/manage/account/token/
# Starts with "pypi-"
PYPI_TOKEN=

# Publish target environment (optional)
# Values: development | production
# Determines which .env file the publish script loads
PUBLISH_ENV=development
